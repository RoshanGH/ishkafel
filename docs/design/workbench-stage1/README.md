# 设计稿 · 审片台 阶段①「切分确认」

- **定稿日期**：2026-07-29（视觉基准，后续页面沿用此设计语言）
- **预览**：[审片台-阶段1-切分确认.png](./审片台-阶段1-切分确认.png)
- **源文件**：[审片台-阶段1-切分确认.html](./审片台-阶段1-切分确认.html)（浏览器直接打开即可，帧图为相对路径引用）
- **设计标准**：Apple HIG + 剪映专业版布局逻辑，详见根目录 `CLAUDE.md`

## 布局结构

- 顶栏：成片信息（名称/来源 miaoa ID/时长/分辨率/标签组）+ 三阶段步骤条
- 上区三栏：台词语义单元列表（320px）｜竖屏播放器（窄舞台居中 + 原片/替换预览分段控件）｜属性检查器（起止时间步进、台词编辑、标签、拆分/合并）
- 时间线（主操作区，约 40% 高）：工具条（撤销/分割/吸附字幕边界/缩放）+ 语义单元色块轨 + 抽帧画面轨 + 音频波形轨 + 播放头
- 底部：状态汇总 + 「确认切分，进入替换选材」主按钮

## 素材说明

画面缩略图与预览帧来自真实成片《JC_滴露_植源喷雾_XCT_千川直播》抽帧；台词文案为示意（实际由 ASR 生成）。
