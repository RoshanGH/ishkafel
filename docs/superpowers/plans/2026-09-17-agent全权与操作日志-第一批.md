# Agent 全权与操作日志 · 第一批（期一~期三）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal：** 把软件里 18 处「Agent 拿不到权限」的代码清零，同时上线一条分得清人和
Agent 的改动日志——删锁之前先让日志能说真话。

**Architecture：** 三层，自下而上做。最底下是 `TaskLogFile`（一条任务一份 jsonl，
只记写操作）；中间是 `TaskMutation`——**唯一的任务写入口**，它做三件事：落盘前重读
一次、只合并调用方动过的字段、把这一笔追加进日志并在任务上盖一个来源戳；最上面才
是删锁，因为锁一删两边就真能同时写，那时候 `TaskMutation` 必须已经就位。

**Tech Stack：** Dart / Flutter，`flutter test` 跑测试，测试用 `flutter_test`。
存储一律走盘上的文件（CLI 与 GUI 是两个进程，磁盘是它们唯一的公共地面）。

**Spec：** `docs/superpowers/specs/2026-09-17-agent全权与操作日志-design.md`

**这一批不包含**（各自成篇，做完这批再出计划）：

- 期四：`ishkafel where` + `arrivedBy` + 返回信封（设计文档第四、五节）
- 期五：跳来跳去（设计文档第八节）

## Global Constraints

- **回复、注释、提交信息一律中文**（项目 CLAUDE.md）。
- **软件不许对 Agent 说「不行」**。命令失败的理由只能是 `exitBadUsage` /
  `exitNotFound`（参数不对）、`exitEnv`（外部依赖真的坏了）、`exitFailed`
  （干了没成）。**没有第四类**——「有人占着」「界面在别处」「界面没回应」一条都
  不许留。
- **软件只报事实，不给规则**。日志和来源戳只说「这是人改的」，绝不说「人改过的
  不许动」。绕开还是照改是 Agent 的判断。
- **取法收敛成一处**。日志写入口、来源戳、任务落盘，各只有一处实现，界面和 CLI 都
  走它。这个项目在「同一件事两处算」上栽过不止三次。
- **按 uid 记，不按下标**。任务里按单元下标记的数据已经有三份
  （`replacements` / `voices` / `bgm`），新增第四份必须先改
  `lib/core/editing/blank_unit_removal.dart` 与 `lib/core/editing/unit_reorder.dart`
  的补偿函数。本计划因此一律按 `SemanticUnit.uid` 记，绕开这一坑。
- **文件 200-400 行为宜，800 行封顶**；函数小于 50 行。
- **不静默降级**。读不懂的日志文件当作空日志并出声告警，绝不吞掉。
- 构建走脚本：`./scripts/build_macos.sh --release` 或 `./scripts/pack.sh`，
  **绝不裸跑 `flutter build`**。裸 `flutter` 只许用于 `analyze` / `test`。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `lib/core/storage/task_log.dart`（新） | 一条日志长什么样 + 一条任务一份 jsonl 的读写。只管记和读，不懂任务模型 |
| `lib/core/storage/edit_stamp.dart`（新） | 来源戳：`{by, at}`，以及它在任务上的 key 约定（按 uid） |
| `lib/core/storage/task_mutation.dart`（新） | **唯一写入口**：重读 → 交给调用方改 → 落盘 → 记日志 → 盖戳 |
| `lib/cli/commands/log_command.dart`（新） | `ishkafel log` |
| `lib/core/models/renew_task.dart`（改） | 加 `editedBy` 字段 |
| `lib/cli/commands/*.dart`（改，13 处） | 去掉 `lock.acquire`，改走 `TaskMutation` |
| `lib/features/{workbench,director,review}/*`（改，3 处） | 去掉 `lock.acquire` 与锁横幅，改走 `TaskMutation` |
| 删除 | `task_lock.dart`、`lock_yield.dart`、`gui_lock_guidance.dart`、`agent_lock_holder.dart`、`task_lock_banner.dart`、`exitLocked`、`UiAction.lockYield` |
| `test/architecture/*`（新 2 个 / 删 2 个） | 守住「没有权限类失败」「所有写入走 TaskMutation」 |

---

## 期一 + 期二：唯一写入口 + 日志 + 来源戳

### Task 1: 日志的记录与落盘

**Files:**
- Create: `lib/core/storage/task_log.dart`
- Create: `test/support/seed_task.dart`（后面 Task 2/3/4/5 都用它建测试任务）
- Test: `test/core/storage/task_log_test.dart`

**Interfaces:**
- Consumes: 无
- Produces:
  - `Future<RenewTask> seedTask(Directory dataDir, {String id, String name})`（测试辅助）
  - `enum ActorKind { human, agent }`
  - `class TaskLogEntry { int seq; DateTime at; ActorKind by; String actor; String taskId; String op; Map<String,dynamic> where; Map<String,dynamic>? before; Map<String,dynamic>? after; String note; Map<String,dynamic> toJson(); static TaskLogEntry? tryFromJson(Object?) }`
  - `class TaskLogFile { TaskLogFile({required Directory dataDir, required String taskId}); int append({required ActorKind by, required String actor, required String op, Map<String,dynamic> where, Map<String,dynamic>? before, Map<String,dynamic>? after, String note}); List<TaskLogEntry> read({int? since, ActorKind? by, int limit}); int get latestSeq; void deleteAll(); }`

- [ ] **Step 1a: 先写共用的测试辅助**

后面四个任务都要造一条真任务再往上加日志。**一处写好，别各写各的**——
那正是这个项目栽过三次的毛病。

```dart
// test/support/seed_task.dart
import 'dart:io';

import 'package:ishkafel/core/models/renew_task.dart';
import 'package:ishkafel/core/storage/file_task_repository.dart';

/// 造一条最小可用的任务并落盘，返回它。
///
/// 字段照 `test/cli/clean_command_test.dart` 里那份最小构造——只给必填的五个，
/// 多给一个字段就多一处将来要跟着模型改的地方。
Future<RenewTask> seedTask(
  Directory dataDir, {
  String id = 't_1',
  String name = '测试任务',
}) async {
  final task = RenewTask(
    id: id,
    name: name,
    status: RenewTaskStatus.ready,
    createdAt: DateTime.now(),
    updatedAt: DateTime.now(),
  );
  await FileTaskRepository(dataDir).save(task);
  return task;
}
```

- [ ] **Step 1b: 写失败的测试**

```dart
// test/core/storage/task_log_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:ishkafel/core/storage/task_log.dart';

/// 日志是这次协作模型的地基：锁删掉之后，「谁在什么时候改了什么」只剩它回答。
/// 它记漏一笔，Agent 查到的「什么都没发生」看起来正好像「一切正常」。
void main() {
  late Directory dataDir;

  setUp(() => dataDir = Directory.systemTemp.createTempSync('ishkafel_log'));
  tearDown(() => dataDir.deleteSync(recursive: true));

  TaskLogFile fileOf(String taskId) =>
      TaskLogFile(dataDir: dataDir, taskId: taskId);

  test('seq 从 1 开始，逐笔递增', () {
    final log = fileOf('t1');
    expect(log.latestSeq, 0);
    expect(log.append(by: ActorKind.agent, actor: 'Agent', op: 'shot.pick'), 1);
    expect(log.append(by: ActorKind.human, actor: '人（工作台）', op: 'shot.remove'), 2);
    expect(log.latestSeq, 2);
  });

  test('新进程读到的 seq 接着上一次往下，不从 1 重来', () {
    fileOf('t1').append(by: ActorKind.agent, actor: 'Agent', op: 'shot.pick');
    // 换一个实例 = 换一个进程
    expect(fileOf('t1').append(by: ActorKind.agent, actor: 'Agent', op: 'x'), 2);
  });

  test('判断依据原样存下来，不被压扁成 id', () {
    final log = fileOf('t1');
    log.append(
      by: ActorKind.human,
      actor: '人（工作台）',
      op: 'shot.remove',
      where: {'unitUid': 'u-abc', 'shot': 1},
      before: {
        'materialId': 105475,
        'tags': ['实拍', '产品特写', '手持'],
        'durationMs': 2400,
        'sceneDescription': '手持喷瓶对着灶台喷洒，画面右下有平台水印',
      },
      note: '人在时间线上右键删除',
    );
    final got = log.read().single;
    expect(got.by, ActorKind.human);
    expect(got.where['unitUid'], 'u-abc');
    expect(got.before!['tags'], ['实拍', '产品特写', '手持']);
    expect(got.before!['sceneDescription'], contains('平台水印'));
  });

  test('--since 只给之后的，--by 只给那一方', () {
    final log = fileOf('t1');
    log.append(by: ActorKind.agent, actor: 'Agent', op: 'a');   // 1
    log.append(by: ActorKind.human, actor: '人', op: 'b');       // 2
    log.append(by: ActorKind.agent, actor: 'Agent', op: 'c');   // 3
    expect(log.read(since: 1).map((e) => e.op), ['b', 'c']);
    expect(log.read(by: ActorKind.human).map((e) => e.op), ['b']);
    expect(log.read(since: 1, by: ActorKind.agent).map((e) => e.op), ['c']);
  });

  test('两条任务的日志互不串台', () {
    fileOf('t1').append(by: ActorKind.agent, actor: 'Agent', op: 'a');
    expect(fileOf('t2').latestSeq, 0);
    expect(fileOf('t2').read(), isEmpty);
  });

  test('坏行跳过，其余照读——一条坏记录不许把整份日志废掉', () {
    final log = fileOf('t1');
    log.append(by: ActorKind.agent, actor: 'Agent', op: 'a');
    File('${dataDir.path}/logs/t1.jsonl')
        .writeAsStringSync('{这不是 json\n', mode: FileMode.append);
    log.append(by: ActorKind.agent, actor: 'Agent', op: 'c');
    expect(log.read().map((e) => e.op), ['a', 'c']);
  });

  test('deleteAll 把这条任务的日志清掉——任务删了不留孤儿', () {
    final log = fileOf('t1');
    log.append(by: ActorKind.agent, actor: 'Agent', op: 'a');
    log.deleteAll();
    expect(log.read(), isEmpty);
    expect(log.latestSeq, 0);
  });
}
```

- [ ] **Step 2: 跑一遍，确认它失败**

Run: `flutter test test/core/storage/task_log_test.dart`
Expected: FAIL —— `Error: Couldn't resolve the package 'ishkafel/core/storage/task_log.dart'`

- [ ] **Step 3: 写实现**

```dart
// lib/core/storage/task_log.dart
import 'dart:convert';
import 'dart:io';

import 'package:path/path.dart' as p;

import '../log/app_log.dart';

/// 这一笔是人干的还是 Agent 干的。
///
/// **产品负责人点名要的**：「这个日志是要区分人的操作和 Agent 操作的」。
/// 只有两个值——第三个值（比如「系统」）会让 Agent 多一类要猜的东西，
/// 而后台管线跑的活儿本来就是 Agent 让它跑的，算 Agent 的。
enum ActorKind { human, agent }

/// 日志里的一笔。
///
/// **`before` / `after` 放的是事实，不是 id。** 判据是产品负责人举的例子：
/// Agent 挑了四五个分镜、人删了其中两个，Agent 要能只凭日志看出「人不要的是
/// 产品特写那一类」。只记「删除 U3S1」它什么也看不出来；把素材的标签、时长、
/// 画面描述、烧没烧字一起记下，意图就在那儿了。
class TaskLogEntry {
  /// 单调递增，是 `--since` 的游标
  final int seq;
  final DateTime at;
  final ActorKind by;

  /// 具体是谁：`人（工作台）` / `Agent`。出问题时要说得出名字
  final String actor;
  final String taskId;

  /// 干了什么：`shot.pick` / `shot.remove` / `unit.segment` …
  final String op;

  /// 动的是哪儿。**按 uid 记，不按下标**——下标会因为删单元、挪单元而漂
  final Map<String, dynamic> where;

  final Map<String, dynamic>? before;
  final Map<String, dynamic>? after;

  /// 人话补一句：「人在时间线上右键删除」
  final String note;

  const TaskLogEntry({
    required this.seq,
    required this.at,
    required this.by,
    required this.actor,
    required this.taskId,
    required this.op,
    this.where = const {},
    this.before,
    this.after,
    this.note = '',
  });

  Map<String, dynamic> toJson() => {
        'seq': seq,
        'at': at.toIso8601String(),
        'by': by.name,
        'actor': actor,
        'task': taskId,
        'op': op,
        if (where.isNotEmpty) 'where': where,
        if (before != null) 'before': before,
        if (after != null) 'after': after,
        if (note.isNotEmpty) 'note': note,
      };

  /// 宽松解析：**任何一处不对就返回 null**，由调用方跳过这一行。
  /// 一行读不懂的记录不该把整份日志废掉
  static TaskLogEntry? tryFromJson(Object? raw) {
    if (raw is! Map) return null;
    final seq = raw['seq'];
    final at = DateTime.tryParse('${raw['at']}');
    final op = raw['op'];
    if (seq is! int || at == null || op is! String || op.isEmpty) return null;
    return TaskLogEntry(
      seq: seq,
      at: at,
      by: ActorKind.values.firstWhere((k) => k.name == raw['by'],
          orElse: () => ActorKind.agent),
      actor: raw['actor'] is String ? raw['actor'] as String : '',
      taskId: raw['task'] is String ? raw['task'] as String : '',
      op: op,
      where: raw['where'] is Map
          ? Map<String, dynamic>.from(raw['where'] as Map)
          : const {},
      before: raw['before'] is Map
          ? Map<String, dynamic>.from(raw['before'] as Map)
          : null,
      after: raw['after'] is Map
          ? Map<String, dynamic>.from(raw['after'] as Map)
          : null,
      note: raw['note'] is String ? raw['note'] as String : '',
    );
  }
}

/// 一条任务一份日志：`<dataDir>/logs/<taskId>.jsonl`，一行一条。
///
/// **用文件而不是内存**：CLI 与 GUI 是两个进程，磁盘是它们唯一的公共地面。
/// 这和任务锁当初选文件是同一个理由——只不过锁要删了，日志留下。
///
/// **跟着任务走**：任务删了日志一起删（[deleteAll]），不留孤儿数据。
class TaskLogFile {
  final Directory dataDir;
  final String taskId;

  TaskLogFile({required this.dataDir, required this.taskId});

  File get _file => File(p.join(dataDir.path, 'logs', '$taskId.jsonl'));

  /// 现在记到第几条。文件不在、读不动都返回 0
  int get latestSeq {
    final entries = read(limit: 1 << 30);
    return entries.isEmpty ? 0 : entries.last.seq;
  }

  /// 记一笔，返回它的 seq。
  ///
  /// **追加写**：并发追加时各自成行，不会互相截断（同一条任务同时有两个写入方
  /// 正是锁删掉之后的常态）。
  int append({
    required ActorKind by,
    required String actor,
    required String op,
    Map<String, dynamic> where = const {},
    Map<String, dynamic>? before,
    Map<String, dynamic>? after,
    String note = '',
  }) {
    final seq = latestSeq + 1;
    final entry = TaskLogEntry(
      seq: seq,
      at: DateTime.now(),
      by: by,
      actor: actor,
      taskId: taskId,
      op: op,
      where: where,
      before: before,
      after: after,
      note: note,
    );
    try {
      final f = _file;
      f.parent.createSync(recursive: true);
      f.writeAsStringSync('${jsonEncode(entry.toJson())}\n',
          mode: FileMode.append, flush: true);
    } catch (e) {
      // **出声，不吞。** 日志记漏一笔，Agent 查到的「什么都没发生」
      // 看起来正好像「一切正常」
      AppLog.warn('改动日志写不进去（$taskId · $op）：$e');
    }
    return seq;
  }

  /// 读。`since` 之后的、`by` 那一方的，最多 `limit` 条（取最近的）
  List<TaskLogEntry> read({int? since, ActorKind? by, int limit = 200}) {
    final f = _file;
    if (!f.existsSync()) return const [];
    final out = <TaskLogEntry>[];
    try {
      for (final line in f.readAsLinesSync()) {
        if (line.trim().isEmpty) continue;
        TaskLogEntry? entry;
        try {
          entry = TaskLogEntry.tryFromJson(jsonDecode(line));
        } catch (_) {
          entry = null;
        }
        // 读不懂的那一行跳过就是了，别让它废掉整份日志
        if (entry == null) continue;
        if (since != null && entry.seq <= since) continue;
        if (by != null && entry.by != by) continue;
        out.add(entry);
      }
    } catch (e) {
      AppLog.warn('改动日志读不动（$taskId）：$e');
      return const [];
    }
    return out.length <= limit ? out : out.sublist(out.length - limit);
  }

  /// 任务删了就把日志一起删——不留孤儿数据
  void deleteAll() {
    try {
      final f = _file;
      if (f.existsSync()) f.deleteSync();
    } catch (e) {
      AppLog.warn('改动日志删不掉（$taskId）：$e');
    }
  }
}
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `flutter test test/core/storage/task_log_test.dart`
Expected: PASS，7 个测试全绿

- [ ] **Step 5: 提交**

```bash
git add lib/core/storage/task_log.dart test/core/storage/task_log_test.dart
git commit -m "feat: 改动日志的记录与落盘

一条任务一份 jsonl，只记写操作，分得清人和 Agent。before/after 存的是
判断依据（标签、时长、画面描述）而不是 id——判据是产品负责人举的例子：
Agent 要能只凭日志看出人删掉的那两个分镜是哪一类。"
```

---

### Task 2: 任务删除时把日志一起删掉

**Files:**
- Modify: `lib/cli/commands/task_command.dart`（`runTaskDeleteCommand`）
- Modify: `lib/features/tasks/task_list_page.dart`（界面删任务处）
- Test: `test/cli/task_delete_clears_log_test.dart`

**Interfaces:**
- Consumes: `TaskLogFile.deleteAll()`（Task 1）
- Produces: 无新接口

- [ ] **Step 1: 写失败的测试**

```dart
// test/cli/task_delete_clears_log_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:ishkafel/cli/commands/task_command.dart';
import 'package:ishkafel/core/storage/task_log.dart';

import '../support/seed_task.dart';

/// 「不留孤儿数据」——磁盘上躺着的每一份数据都要有人读、有人删。
/// 任务没了，它的日志再没人会去看，留着就是垃圾。
void main() {
  test('删任务之后日志文件不在了', () async {
    final dataDir = Directory.systemTemp.createTempSync('ishkafel_del');
    addTearDown(() => dataDir.deleteSync(recursive: true));

    final task = await seedTask(dataDir);
    TaskLogFile(dataDir: dataDir, taskId: task.id)
        .append(by: ActorKind.agent, actor: 'Agent', op: 'shot.pick');
    expect(File('${dataDir.path}/logs/${task.id}.jsonl').existsSync(), isTrue);

    await runTaskDeleteCommand(
        rest: [task.id], dataDir: dataDir, yes: true, err: StringBuffer());

    expect(File('${dataDir.path}/logs/${task.id}.jsonl').existsSync(), isFalse,
        reason: '任务删了日志还躺在盘上，就是孤儿数据');
  });
}
```

> `runTaskDeleteCommand` 的真实参数名以 `lib/cli/commands/task_command.dart`
> 为准（`--yes` 那个开关现在叫什么就写什么）——**别照记忆里的名字写**，
> 手册和实际对不上是这个项目反复栽过的一类。

- [ ] **Step 2: 跑，确认失败**

Run: `flutter test test/cli/task_delete_clears_log_test.dart`
Expected: FAIL —— 日志文件仍然存在

- [ ] **Step 3: 在两个删除入口都接上**

CLI 侧（`runTaskDeleteCommand` 里 `repository.delete(id)` 之后）：

```dart
TaskLogFile(dataDir: dataDir, taskId: task.id).deleteAll();
```

界面侧（`task_list_page.dart` 删任务确认之后，同一位置）加同一行。

- [ ] **Step 4: 跑测试，确认通过**

Run: `flutter test test/cli/task_delete_clears_log_test.dart`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "fix: 删任务时把改动日志一起删掉

不留孤儿数据：任务没了，它的日志再没人会去看。CLI 和界面两个删除入口
都要接上。"
```

---

### Task 3: 来源戳

**Files:**
- Create: `lib/core/storage/edit_stamp.dart`
- Modify: `lib/core/models/renew_task.dart`（加 `editedBy` 字段 + `copyWith` + `toJson` / `fromJson`）
- Test: `test/core/storage/edit_stamp_test.dart`

**Interfaces:**
- Consumes: `ActorKind`（Task 1）
- Produces:
  - `class EditStamp { ActorKind by; DateTime at; Map<String,dynamic> toJson(); static EditStamp? tryFromJson(Object?); }`
  - `String stampKeyForUnit(String uid)` → `"u:<uid>"`
  - `String stampKeyForShot(String uid, int shotIndex)` → `"u:<uid>/s:<i>"`
  - `RenewTask.editedBy` → `Map<String, EditStamp>`

**为什么要它**（写进文件头注释）：产品负责人的原话是「**如果你发现这个是人已经
修改的**」。「发现」要求 Agent 看当前数据时就能看见，而日志是时间线、要翻。两者
分工：日志回答「发生过什么」，来源戳回答「我现在看到的这一处是谁定的」。

**为什么按 uid 不按下标**：任务里按单元下标记的数据已经有三份
（`replacements` / `voices` / `bgm`），每加一份都要改
`blank_unit_removal.dart` / `unit_reorder.dart` 两个补偿函数，而且漏过一次
（配音跑到别的单元身上，不报错，只有听出来才知道）。按 uid 记就不用进那两个文件。

- [ ] **Step 1: 写失败的测试**

```dart
// test/core/storage/edit_stamp_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:ishkafel/core/storage/edit_stamp.dart';
import 'package:ishkafel/core/storage/task_log.dart';

void main() {
  test('key 按 uid 生成，删单元挪单元都不会漂', () {
    expect(stampKeyForUnit('u-abc'), 'u:u-abc');
    expect(stampKeyForShot('u-abc', 2), 'u:u-abc/s:2');
    // 不含任何单元下标
    expect(stampKeyForShot('u-abc', 2), isNot(contains('unit')));
  });

  test('来回一趟不掉东西', () {
    final at = DateTime.parse('2026-09-17T14:22:07.412Z');
    final s = EditStamp(by: ActorKind.human, at: at);
    final back = EditStamp.tryFromJson(s.toJson())!;
    expect(back.by, ActorKind.human);
    expect(back.at.toUtc(), at);
  });

  test('读不懂就当没有，不让一个坏戳废掉整条任务', () {
    expect(EditStamp.tryFromJson(null), isNull);
    expect(EditStamp.tryFromJson({'by': 'human'}), isNull);       // 缺 at
    expect(EditStamp.tryFromJson({'at': '不是时间'}), isNull);
  });
}
```

再加一条模型侧的：

```dart
// 追加进 test/core/models/renew_task_test.dart（已存在）
test('editedBy 存得住、读得回', () {
  final task = RenewTask(
    id: 't_1',
    name: '测试任务',
    status: RenewTaskStatus.ready,
    createdAt: DateTime.now(),
    updatedAt: DateTime.now(),
    editedBy: {
      'u:u-abc/s:1': EditStamp(by: ActorKind.human, at: DateTime.now()),
    },
  );
  final back = RenewTask.fromJson(task.toJson());
  expect(back.editedBy['u:u-abc/s:1']!.by, ActorKind.human);
});

test('读不懂的戳被丢掉，其余的照常读回来——一个坏戳不许废掉整条任务', () {
  final raw = RenewTask(
    id: 't_1',
    name: '测试任务',
    status: RenewTaskStatus.ready,
    createdAt: DateTime.now(),
    updatedAt: DateTime.now(),
    editedBy: {'u:good': EditStamp(by: ActorKind.agent, at: DateTime.now())},
  ).toJson();
  (raw['editedBy'] as Map)['u:bad'] = {'by': 'human'};   // 缺 at

  final back = RenewTask.fromJson(raw);
  expect(back.editedBy.keys, ['u:good']);
});
```

- [ ] **Step 2: 跑，确认失败**

Run: `flutter test test/core/storage/edit_stamp_test.dart test/core/models/renew_task_test.dart`
Expected: FAIL —— `edit_stamp.dart` 不存在、`editedBy` 未定义

- [ ] **Step 3: 写实现**

```dart
// lib/core/storage/edit_stamp.dart
import 'task_log.dart';

/// 「我现在看到的这一处，是谁定的。」
///
/// 产品负责人的原话是「**如果你发现这个是人已经修改的**」——「发现」这个词要求
/// Agent 看当前数据时就能看见，而不是必须先去翻一遍时间线。所以除了日志
/// （[TaskLogEntry]，回答「发生过什么」），当前数据上还要盖一个戳，
/// 回答「这一处是谁定的」。
///
/// **只标事实，不给规则**：软件不说「人改过的不许动」，只说「这是人改的」。
/// 绕开还是照改是 Agent 的判断。
class EditStamp {
  final ActorKind by;
  final DateTime at;

  const EditStamp({required this.by, required this.at});

  Map<String, dynamic> toJson() =>
      {'by': by.name, 'at': at.toIso8601String()};

  /// 宽松解析：**任何一处不对就返回 null**。一个读不懂的戳不该废掉整条任务
  static EditStamp? tryFromJson(Object? raw) {
    if (raw is! Map) return null;
    final at = DateTime.tryParse('${raw['at']}');
    if (at == null) return null;
    final by = ActorKind.values.firstWhere((k) => k.name == raw['by'],
        orElse: () => ActorKind.agent);
    return EditStamp(by: by, at: at);
  }
}

/// 戳记在哪儿：**一律按 `SemanticUnit.uid`，不按下标**。
///
/// 任务里按单元下标记的数据已经有三份（`replacements` / `voices` / `bgm`），
/// 每加一份都得改 `blank_unit_removal.dart` / `unit_reorder.dart` 两个补偿
/// 函数——而且漏过一次：删掉 U2 之后本该念 U3 的配音跑到了 U2 身上，不报错，
/// 只有听出来才知道。按 uid 记就不用进那两个文件。
String stampKeyForUnit(String uid) => 'u:$uid';

String stampKeyForShot(String uid, int shotIndex) => 'u:$uid/s:$shotIndex';
```

`renew_task.dart` 的改动，四处：

```dart
// 1) 字段（放在 replacementsByUid 附近）
  /// 每一处是谁定的、什么时候定的。key 见 `edit_stamp.dart` 的约定（按 uid）。
  /// Agent 看 `task --json` 时当场就知道自己要动的是谁的东西
  final Map<String, EditStamp> editedBy;

// 2) 构造函数参数
    this.editedBy = const {},

// 3) copyWith
    Map<String, EditStamp>? editedBy,
    ...
    editedBy: editedBy ?? this.editedBy,

// 4) toJson / fromJson
    if (editedBy.isNotEmpty)
      'editedBy': {for (final e in editedBy.entries) e.key: e.value.toJson()},

    editedBy: json['editedBy'] is Map
        ? {
            for (final e in (json['editedBy'] as Map).entries)
              if (EditStamp.tryFromJson(e.value) case final s?) '${e.key}': s,
          }
        : const {},
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `flutter test test/core/storage/edit_stamp_test.dart test/core/models/renew_task_test.dart`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: 任务数据上的来源戳

产品负责人说的是「如果你发现这个是人已经修改的」——「发现」要求看当前数据
就能看见，光有日志（时间线，要翻）不够。按 uid 记不按下标，避开
replacements/voices/bgm 那套下标补偿。"
```

---

### Task 4: 唯一写入口 `TaskMutation`

**Files:**
- Create: `lib/core/storage/task_mutation.dart`
- Test: `test/core/storage/task_mutation_test.dart`

**Interfaces:**
- Consumes: `TaskRepository`、`TaskLogFile`、`ActorKind`、`EditStamp`、`stampKeyForShot`
- Produces:
  - `class TaskEdit { RenewTask task; Map<String,dynamic>? before; Map<String,dynamic>? after; List<String> stampKeys; }`
  - `class TaskMutation { TaskMutation({required TaskRepository repo, required Directory dataDir, required ActorKind by, required String actor}); Future<RenewTask?> apply({required String taskId, required String op, Map<String,dynamic> where, String note, required TaskEdit Function(RenewTask fresh) edit}); }`

**这个类为什么存在**（写进文件头）：两条理由，缺一不可。

1. **写盘前重读。** 任务是整份对象落库，没有字段级合并。Agent 拿五分钟前的快照
   整份写回，把人改的抹掉了——**而它自己不知道它抹了，所以日志里也不会有这一条**。
   日志就成了假账，而假账比没账更糟。锁删掉之后两边真能同时写，这一条是底线。
2. **一处记账。** 日志漏记一笔，Agent 查到的「什么都没发生」看起来正好像
   「一切正常」。所以不给各命令自己去 `append` 的机会。

- [ ] **Step 1: 写失败的测试**

```dart
// test/core/storage/task_mutation_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:ishkafel/core/storage/edit_stamp.dart';
import 'package:ishkafel/core/storage/file_task_repository.dart';
import 'package:ishkafel/core/storage/task_log.dart';
import 'package:ishkafel/core/storage/task_mutation.dart';

import '../../support/seed_task.dart';

/// 锁删掉之后，同一条任务真的会有两个写入方。这个类是那时候唯一不让改动
/// 互相抹掉、也不让日志记成假账的东西。
void main() {
  late Directory dataDir;
  late FileTaskRepository repo;

  setUp(() {
    dataDir = Directory.systemTemp.createTempSync('ishkafel_mut');
    repo = FileTaskRepository(dataDir);
  });
  tearDown(() => dataDir.deleteSync(recursive: true));

  TaskMutation agentMutation() => TaskMutation(
      repo: repo, dataDir: dataDir, by: ActorKind.agent, actor: 'Agent');

  test('交给 edit 的是刚从盘上重读的那份，不是调用方手里的旧快照', () async {
    final stale = await seedTask(dataDir, name: '原名');
    // 别人改了名字（模拟人在 Agent 思考的那 30 秒里动了手）
    await repo.save((await repo.findById(stale.id))!.copyWith(name: '人改过的名字'));

    late String sawName;
    await agentMutation().apply(
      taskId: stale.id,
      op: 'task.touch',
      edit: (fresh) {
        sawName = fresh.name;           // ← 必须是「人改过的名字」
        return TaskEdit(task: fresh);
      },
    );

    expect(sawName, '人改过的名字',
        reason: 'edit 拿到旧快照的话，整份写回就会把人的改动静默抹掉');
  });

  test('人的改动不会被 Agent 的整份写回抹掉', () async {
    final task = await seedTask(dataDir, name: '原名');
    await repo.save((await repo.findById(task.id))!.copyWith(name: '人改过的名字'));

    await agentMutation().apply(
      taskId: task.id,
      op: 'task.note',
      edit: (fresh) => TaskEdit(task: fresh.copyWith(analysisError: 'x')),
    );

    final after = await repo.findById(task.id);
    expect(after!.name, '人改过的名字');   // 没被抹
    expect(after.analysisError, 'x');     // Agent 自己那笔也在
  });

  test('落盘之后才记日志，而且记的是 op / where / before / after', () async {
    final task = await seedTask(dataDir);
    await agentMutation().apply(
      taskId: task.id,
      op: 'shot.pick',
      where: {'unitUid': 'u-abc', 'shot': 1},
      note: '照参考镜挑的',
      edit: (fresh) => TaskEdit(
        task: fresh,
        before: {'materialId': null},
        after: {'materialId': 105475, 'tags': ['实拍', '厨房']},
      ),
    );

    final entry =
        TaskLogFile(dataDir: dataDir, taskId: task.id).read().single;
    expect(entry.by, ActorKind.agent);
    expect(entry.op, 'shot.pick');
    expect(entry.where['unitUid'], 'u-abc');
    expect(entry.after!['tags'], ['实拍', '厨房']);
    expect(entry.note, '照参考镜挑的');
  });

  test('stampKeys 上盖来源戳，Agent 看 task 数据当场知道这是谁定的', () async {
    final task = await seedTask(dataDir);
    await TaskMutation(
            repo: repo, dataDir: dataDir, by: ActorKind.human, actor: '人（工作台）')
        .apply(
      taskId: task.id,
      op: 'shot.remove',
      edit: (fresh) => TaskEdit(
        task: fresh,
        stampKeys: [stampKeyForShot('u-abc', 1)],
      ),
    );

    final after = await repo.findById(task.id);
    expect(after!.editedBy[stampKeyForShot('u-abc', 1)]!.by, ActorKind.human);
  });

  test('任务不在就返回 null，不记日志——找不到是事实，不是权限', () async {
    final got = await agentMutation().apply(
      taskId: '不存在',
      op: 'x',
      edit: (fresh) => TaskEdit(task: fresh),
    );
    expect(got, isNull);
    expect(TaskLogFile(dataDir: dataDir, taskId: '不存在').read(), isEmpty);
  });

  test('edit 抛异常时不落盘、不记日志——记了就是假账', () async {
    final task = await seedTask(dataDir, name: '原名');
    await expectLater(
      agentMutation().apply(
          taskId: task.id, op: 'x', edit: (_) => throw StateError('炸了')),
      throwsStateError,
    );
    expect((await repo.findById(task.id))!.name, '原名');
    expect(TaskLogFile(dataDir: dataDir, taskId: task.id).read(), isEmpty);
  });
}
```

> `seedTask` 来自 Task 1 建的 `test/support/seed_task.dart`——**别再各写一份**。

- [ ] **Step 2: 跑，确认失败**

Run: `flutter test test/core/storage/task_mutation_test.dart`
Expected: FAIL —— `task_mutation.dart` 不存在

- [ ] **Step 3: 写实现**

```dart
// lib/core/storage/task_mutation.dart
import 'dart:io';

import '../models/renew_task.dart';
import 'edit_stamp.dart';
import 'task_log.dart';
import 'task_repository.dart';

/// 一次改动的产物：改完的任务 + 这一笔的判断依据 + 该盖戳的位置。
///
/// `before` / `after` 由调用方给，因为**只有它知道哪些事实值得记**。
/// 判据见 `task_log.dart`：Agent 要能只凭日志看出人删掉的那两个分镜是哪一类。
class TaskEdit {
  final RenewTask task;
  final Map<String, dynamic>? before;
  final Map<String, dynamic>? after;

  /// 这一笔动了哪几处。见 `edit_stamp.dart` 的 key 约定（按 uid）
  final List<String> stampKeys;

  const TaskEdit({
    required this.task,
    this.before,
    this.after,
    this.stampKeys = const [],
  });
}

/// **唯一的任务写入口。** 界面和 CLI 都走它，谁都不许自己 `repo.save`。
///
/// 两条理由，缺一不可：
///
/// 1. **写盘前重读。** 任务是整份对象落库，没有字段级合并。拿五分钟前的快照
///    整份写回，会把这中间别人改的东西抹掉——**而写的人自己不知道它抹了，
///    所以日志里也不会有这一条**。日志就成了假账，而假账比没账更糟。
///    锁删掉之后同一条任务真的会有两个写入方，这一条是底线。
/// 2. **一处记账。** 日志漏记一笔，Agent 查到的「什么都没发生」看起来正好像
///    「一切正常」。所以不给各命令自己去 `append` 的机会。
///
/// 这个项目在「同一件事两处算」上栽过不止三次，日志是第四次的完美候选。
class TaskMutation {
  final TaskRepository repo;
  final Directory dataDir;

  /// 这个写入方是人还是 Agent
  final ActorKind by;

  /// 具体是谁：`人（工作台）` / `Agent`
  final String actor;

  const TaskMutation({
    required this.repo,
    required this.dataDir,
    required this.by,
    required this.actor,
  });

  /// 改一笔。任务不在就返回 null（**那是事实，不是权限**），否则返回改完的任务。
  ///
  /// [edit] 拿到的 `fresh` 是**刚从盘上重读的**那一份，不是调用方手里的旧快照。
  Future<RenewTask?> apply({
    required String taskId,
    required String op,
    Map<String, dynamic> where = const {},
    String note = '',
    required TaskEdit Function(RenewTask fresh) edit,
  }) async {
    final fresh = await repo.findById(taskId);
    if (fresh == null) return null;

    // edit 抛出来就让它抛：没落盘就不该记日志，记了就是假账
    final result = edit(fresh);

    final now = DateTime.now();
    final stamped = result.stampKeys.isEmpty
        ? result.task
        : result.task.copyWith(editedBy: {
            ...result.task.editedBy,
            for (final k in result.stampKeys)
              k: EditStamp(by: by, at: now),
          });

    await repo.save(stamped);

    // **落盘之后才记**：报的是存进去的东西，不是打算存的东西
    TaskLogFile(dataDir: dataDir, taskId: taskId).append(
      by: by,
      actor: actor,
      op: op,
      where: where,
      before: result.before,
      after: result.after,
      note: note,
    );
    return stamped;
  }
}
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `flutter test test/core/storage/task_mutation_test.dart`
Expected: PASS，6 个测试全绿

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: 唯一任务写入口 TaskMutation

落盘前重读 + 只合并调用方动过的字段 + 落盘后记日志 + 盖来源戳。

锁删掉之后同一条任务真的会有两个写入方，拿旧快照整份写回会把别人的改动
静默抹掉，而写的人自己不知道——日志就成了假账。这个类是那时候唯一不让
这件事发生的东西。"
```

---

### Task 5: `ishkafel log` 命令

**Files:**
- Create: `lib/cli/commands/log_command.dart`
- Modify: `bin/ishkafel.dart`（注册子命令与参数）
- Modify: `docs/AGENT_SKILL.md`（新增一节）
- Test: `test/cli/log_command_test.dart`

**Interfaces:**
- Consumes: `TaskLogFile.read()`、`resolveTaskRef`
- Produces: `Future<int> runLogCommand({required List<String> rest, required Directory dataDir, int? since, String? by, bool json = true, int limit = 200, StringSink? out, StringSink? err})`

- [ ] **Step 1: 写失败的测试**

```dart
// test/cli/log_command_test.dart
import 'dart:convert';
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:ishkafel/cli/cli_output.dart';
import 'package:ishkafel/cli/commands/log_command.dart';
import 'package:ishkafel/core/storage/task_log.dart';

import '../support/seed_task.dart';

void main() {
  late Directory dataDir;
  setUp(() => dataDir = Directory.systemTemp.createTempSync('ishkafel_logcmd'));
  tearDown(() => dataDir.deleteSync(recursive: true));

  test('--json 一行一条，字段名和盘上一致', () async {
    final taskId = (await seedTask(dataDir)).id;
    TaskLogFile(dataDir: dataDir, taskId: taskId).append(
      by: ActorKind.human,
      actor: '人（工作台）',
      op: 'shot.remove',
      where: {'unitUid': 'u-abc', 'shot': 1},
      before: {'materialId': 105475, 'tags': ['实拍', '产品特写']},
    );

    final out = StringBuffer();
    final code = await runLogCommand(rest: [taskId], dataDir: dataDir, out: out);

    expect(code, 0);
    final rows = const LineSplitter().convert(out.toString().trim());
    final row = jsonDecode(rows.single) as Map<String, dynamic>;
    expect(row['by'], 'human');
    expect(row['op'], 'shot.remove');
    expect((row['before'] as Map)['tags'], ['实拍', '产品特写']);
  });

  test('--by human 只给人干的', () async {
    final taskId = (await seedTask(dataDir)).id;
    final log = TaskLogFile(dataDir: dataDir, taskId: taskId);
    log.append(by: ActorKind.agent, actor: 'Agent', op: 'a');
    log.append(by: ActorKind.human, actor: '人', op: 'b');

    final out = StringBuffer();
    await runLogCommand(
        rest: [taskId], dataDir: dataDir, by: 'human', out: out);
    final rows = const LineSplitter().convert(out.toString().trim());
    expect(rows, hasLength(1));
    expect((jsonDecode(rows.single) as Map)['op'], 'b');
  });

  test('任务不存在 → exitNotFound，报错说得出是哪个 id', () async {
    final err = StringBuffer();
    final code =
        await runLogCommand(rest: ['没这条'], dataDir: dataDir, err: err);
    expect(code, exitNotFound);
    expect(err.toString(), contains('没这条'));
  });

  test('--by 写错不是「没结果」，是当场点名', () async {
    final taskId = (await seedTask(dataDir)).id;
    final err = StringBuffer();
    final code = await runLogCommand(
        rest: [taskId], dataDir: dataDir, by: '人类', err: err);
    expect(code, exitBadUsage);
    expect(err.toString(), contains('human'));
    expect(err.toString(), contains('agent'));
  });

  test('还没有任何改动时，说「还没有改动」而不是空着', () async {
    final taskId = (await seedTask(dataDir)).id;
    final out = StringBuffer();
    final err = StringBuffer();
    final code = await runLogCommand(
        rest: [taskId], dataDir: dataDir, json: false, out: out, err: err);
    expect(code, 0);
    expect('${out.toString()}${err.toString()}', contains('还没有改动'));
  });
}
```

- [ ] **Step 2: 跑，确认失败**

Run: `flutter test test/cli/log_command_test.dart`
Expected: FAIL —— `log_command.dart` 不存在

- [ ] **Step 3: 写实现**

```dart
// lib/cli/commands/log_command.dart
import 'dart:io';

import '../../core/storage/file_task_repository.dart';
import '../../core/storage/task_log.dart';
import '../../core/storage/task_seq.dart';
import '../cli_output.dart';

/// `ishkafel log <任务>` —— **我不在的时候，这条任务上发生了什么。**
///
/// Agent 每次断开重连读它一遍，就能接上原来的活儿，甚至看出人没说出口的意思：
/// 它挑了四五个分镜、人删了其中两个，光看「删了两个」什么也读不出来，
/// 而把被删那两条的标签、时长、画面描述摆出来，「人不要产品特写那一类」
/// 就自己浮上来了。
///
/// **只记写操作。** 只读的一律不记——人翻了二十个任务又翻回来，那是「现状」
/// 不是「历史」，`ishkafel where` 回答它。
Future<int> runLogCommand({
  required List<String> rest,
  required Directory dataDir,

  /// 我上次看到这个游标，给我后面的
  int? since,

  /// 只看 `human` 或 `agent` 干的
  String? by,
  bool json = true,
  int limit = 200,
  StringSink? out,
  StringSink? err,
}) async {
  final sink = err ?? stderr;
  if (rest.isEmpty) {
    sink.writeln('要指定任务：\n'
        '  ishkafel log <任务>                 # 人看的版式\n'
        '  ishkafel log <任务> --json          # 一行一条 JSON\n'
        '  ishkafel log <任务> --since <游标>   # 我上次看到这儿，后面呢\n'
        '  ishkafel log <任务> --by human      # 只看人干了什么');
    return exitBadUsage;
  }
  final task = await resolveTaskRef(FileTaskRepository(dataDir), rest.first);
  if (task == null) {
    sink.writeln('没有这个任务：${rest.first}');
    return exitNotFound;
  }

  // 写错的过滤条件要当场点名。默默返回空列表的话，人和 Agent 都会
  // 以为「真的什么都没发生」——那是这个项目栽过的「三态混成两态」
  ActorKind? filter;
  if (by != null && by.trim().isNotEmpty) {
    filter = ActorKind.values
        .where((k) => k.name == by.trim().toLowerCase())
        .firstOrNull;
    if (filter == null) {
      sink.writeln('--by 要是 human 或 agent 之一（你给的是「$by」）');
      return exitBadUsage;
    }
  }

  final entries = TaskLogFile(dataDir: dataDir, taskId: task.id)
      .read(since: since, by: filter, limit: limit);

  if (entries.isEmpty) {
    // 「没有改动」和「没查成」要分得开：这里是前者，明说
    sink.writeln('这条任务还没有改动记录'
        '${since == null ? '' : '（游标 $since 之后）'}。');
    if (json) emitJson({'ok': true, 'entries': 0, 'cursor': since ?? 0}, out: out);
    return 0;
  }

  if (json) {
    for (final e in entries) {
      emitJson(e.toJson(), out: out);
    }
    return 0;
  }

  final w = out ?? stdout;
  for (final e in entries.reversed) {
    final who = e.by == ActorKind.human ? '人' : 'Agent';
    w.writeln('#${e.seq}  ${_hhmm(e.at)}  $who  ${e.op}'
        '${e.where.isEmpty ? '' : '  ${e.where}'}');
    if (e.note.isNotEmpty) w.writeln('        ${e.note}');
    for (final entry in {...?e.before, ...?e.after}.entries) {
      w.writeln('        ├ ${entry.key}：${entry.value}');
    }
  }
  return 0;
}

String _hhmm(DateTime at) =>
    '${at.hour.toString().padLeft(2, '0')}:'
    '${at.minute.toString().padLeft(2, '0')}';
```

`bin/ishkafel.dart` 里照现有子命令的写法注册 `log`，参数 `--since`（int）、
`--by`（String）、`--no-json`（`status` 已有这个开关，用同一个 flag 名）、`--limit`（int）。

- [ ] **Step 4: 跑测试，确认通过**

Run: `flutter test test/cli/log_command_test.dart`
Expected: PASS，5 个测试全绿

- [ ] **Step 5: 手跑一次真命令**

> 这一步不许省。记忆里那条：外部命令的参数至少手跑一次——假 runner 验不出
> 参数名写错（`codesign -q` 那次）。

```bash
./scripts/build_macos.sh --release   # 或用已有的 ishkafel 可执行文件
ishkafel log <任意一条真任务>
ishkafel log <任意一条真任务> --json | head -3
ishkafel log <任意一条真任务> --by human
```

Expected: 三条都不报错；`--by 人类` 要当场报错并说出 human/agent。

- [ ] **Step 6: 写进手册**

`docs/AGENT_SKILL.md` 新增一节，内容**只给事实和出路，不给规则**：

```markdown
## 我不在的时候发生了什么

ishkafel log <任务>                  # 人看的版式
ishkafel log <任务> --json           # 一行一条
ishkafel log <任务> --since <游标>    # 我上次看到这儿，后面呢
ishkafel log <任务> --by human       # 只看人干了什么

日志**只记改动**，人的和你的都记，`by` 字段分得开。只读的翻页、看候选一律不记
——那是「现状」，`ishkafel where` 回答。

任务数据里每一处还带一个 `editedBy`：这一处是谁定的、什么时候定的。
`task --json` 里就有，不用先去翻日志。

**撞见「这一处是人改的」之后怎么办，软件不管。** 绕开它、照改、还是先跟人说
一声，你自己判断。要改的话把理由播出来，人在旁边看得见。
```

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat: ishkafel log —— 我不在的时候发生了什么

Agent 每次断开重连读它一遍就能接上活儿。--since 给游标、--by 分人和 Agent。
过滤条件写错当场点名，不返回空列表冒充「什么都没发生」。"
```

---

### Task 6: 把 CLI 的写入点迁到 `TaskMutation`

**Files:**
- Modify: `lib/cli/commands/` 下所有 `repo.save(` / `repository.save(` 的调用点
- Test: `test/architecture/one_task_writer_test.dart`（新）

**Interfaces:**
- Consumes: `TaskMutation.apply`（Task 4）
- Produces: 无新接口

**做法**：逐个命令把

```dart
await repo.save(task.copyWith(units: newUnits));
```

改成

```dart
await TaskMutation(
  repo: repo, dataDir: dataDir, by: ActorKind.agent, actor: 'Agent',
).apply(
  taskId: task.id,
  op: 'unit.segment',
  where: {'unitUid': unit.uid},
  edit: (fresh) => TaskEdit(
    task: fresh.copyWith(units: _segment(fresh.units!, unit.uid)),
    before: {'shotCount': 0},
    after: {'shotCount': newShots.length},
    stampKeys: [stampKeyForUnit(unit.uid)],
  ),
);
```

**三条硬要求**：

1. `edit` 里**只能读 `fresh`**，不许引用外层那个旧 `task` 变量。这是整个改造
   的要点——引用了旧变量就等于什么都没改。
2. `op` 用点号分段的短名：`shot.pick` / `shot.remove` / `unit.segment` /
   `unit.reorder` / `voice.assign` / `bgm.set` / `subtitle.set` / `plans.apply`。
3. `before` / `after` 放**判断依据**，不是 id。挑素材要带 `tags`、`durationMs`、
   `sceneDescription`、`burnedText`、`productBrand`；改时长要带前后毫秒数和变速比。

- [ ] **Step 1: 先写架构测试（它现在会红，那正是这个任务的清单）**

```dart
// test/architecture/one_task_writer_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';

/// **同一条任务不能有两个各自写盘的写入方。**
///
/// 任务是整份对象存盘，没有字段级合并——后写的那个会把先写的改动整片盖掉，
/// 而且不报错。这个形状在这个项目里已经撞见过五次（管线打标 vs 工作台编辑、
/// 审核页 vs 工作台、设置页三层缓存、按下标记的三份数据、Agent 的旧快照）。
///
/// 锁删掉之后两边真的会同时写，所以从这里开始**只允许一个写入口**：
/// `TaskMutation`。它落盘前重读、只合并调用方动过的字段、落盘后记日志。
void main() {
  /// 白名单：这几处是写入口自己，或者根本不改已存在的任务
  const allowed = {
    'lib/core/storage/task_mutation.dart',   // 写入口本人
    'lib/core/storage/file_task_repository.dart',
    'lib/core/storage/task_repository.dart',
  };

  test('除了 TaskMutation，没有别的地方直接 save 任务', () {
    final offenders = <String>[];
    for (final f in Directory('lib')
        .listSync(recursive: true)
        .whereType<File>()
        .where((f) => f.path.endsWith('.dart'))) {
      final rel = f.path.replaceFirst('${Directory.current.path}/', '');
      if (allowed.contains(rel)) continue;
      final src = f.readAsStringSync();
      for (final m in RegExp(r'\b(\w+)\.save\(').allMatches(src)) {
        final receiver = m.group(1)!;
        // 只认任务仓库那一类接收者，别把别的 save 误伤了
        if (!RegExp(r'repo|repository|tasks?Repo', caseSensitive: false)
            .hasMatch(receiver)) continue;
        offenders.add('$rel → $receiver.save(');
      }
    }
    expect(offenders, isEmpty,
        reason: '这些地方绕过 TaskMutation 直接写任务——它们会拿旧快照整份'
            '写回，把别人的改动静默抹掉，而且日志里不会有这一条：\n'
            '${offenders.toSet().join('\n')}\n'
            '改成 TaskMutation(...).apply(taskId: …, op: …, edit: (fresh) => …)，'
            '注意 edit 里只能读 fresh，不许引用外层的旧 task');
  });
}
```

- [ ] **Step 2: 跑，看清楚清单有多长**

Run: `flutter test test/architecture/one_task_writer_test.dart`
Expected: FAIL，reason 里列出所有还没迁的位置。**把这份清单抄下来当 todo。**

- [ ] **Step 3: 逐个迁，每迁完一个命令跑一次它自己的测试**

按这个顺序（从简单到复杂，每个都能单独提交）：
`subtitle` → `bgm` → `voice` → `unit` → `blank` → `apply` → `review` →
`script_apply` → `analyze` → `script_run` → `export` → `jianying` →
`voice_file` → `import`

每迁一个：

```bash
flutter test test/cli/<对应的>_test.dart
```

- [ ] **Step 4: 架构测试转绿**

Run: `flutter test test/architecture/one_task_writer_test.dart`
Expected: PASS

- [ ] **Step 5: 全量跑一次，确认没碰坏别的**

Run: `flutter test`
Expected: 全绿

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "refactor: CLI 的任务写入全部走 TaskMutation

每一处改成落盘前重读、只合并自己动过的字段、落盘后记日志、盖来源戳。
加架构测试守住「除了 TaskMutation 没有第二个写入方」——这个形状在这个
项目里撞见过五次，每次都是改动被静默抹掉、日志一片正常。"
```

---

### Task 7: 把界面的写入点迁到 `TaskMutation`

**Files:**
- Modify: `lib/features/workbench/workbench_page.dart`、
  `lib/features/director/director_page.dart`、
  `lib/features/review/review_page.dart`，以及它们调 `save` 的控制器
- Test: 沿用 Task 6 的 `one_task_writer_test.dart`（它扫的是整个 `lib`）

**Interfaces:**
- Consumes: `TaskMutation.apply`
- Produces: 无

界面这边 `by: ActorKind.human`，`actor` 按页面给：`'人（工作台）'` /
`'人（编导台）'` / `'人（审片台）'`。

**要点**：内嵌的子页面（审核页嵌在工作台里）**不自己写盘**，改动经回调交回持有
会话的那一方（现有的 `onTagsChanged` / `onApply` 就是这个形状）——别借着这次改造
让它们各自去调 `TaskMutation`，那是把同一个毛病换个地方再犯一遍。

- [ ] **Step 1: 跑架构测试，看界面这边还剩哪些**

Run: `flutter test test/architecture/one_task_writer_test.dart`
Expected: FAIL，列出 `lib/features/**` 下剩余的位置

- [ ] **Step 2: 逐个迁**

三个页面各自迁完之后跑：

```bash
flutter test test/features/
```

- [ ] **Step 3: 架构测试转绿**

Run: `flutter test test/architecture/one_task_writer_test.dart`
Expected: PASS

- [ ] **Step 4: 真机跑一遍，人手改一处看日志有没有记上**

> UI 改动要真机跑起来截图核对，不允许只报「测试通过」。
> 而且**别在用户的真实数据上做自动点击**——新建一条测试任务自己点。

```bash
./scripts/build_macos.sh --release
open build/macos/Build/Products/Release/ishkafel.app
# 建一条空白任务 → 改一个单元的标签 → 退出
ishkafel log <那条任务> --by human
```

Expected: 看得到那一笔，`by` 是 `human`，`actor` 是「人（工作台）」，
`before` / `after` 里有改前改后的标签。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "refactor: 界面的任务写入全部走 TaskMutation

by=human，actor 按页面给（人（工作台）/ 人（编导台）/ 人（审片台））。
内嵌子页面照旧经回调交回持有会话的那一方，不各自写盘。"
```

---

## 期三：删锁

### Task 8: 委派降级——先问界面在不在，不在就自己干

**Files:**
- Modify: `lib/cli/commands/ui_command.dart`、`lib/cli/commands/apply_command.dart`、
  `lib/cli/commands/review_command.dart`
- Test: `test/cli/delegate_falls_back_test.dart`（新）

**Interfaces:**
- Consumes: `readUiWhere`（已有，`lib/core/storage/ui_where.dart`）
- Produces: `Future<T> delegateOrDoItYourself<T>({required Directory dataDir, required String? taskId, required Future<T?> Function() viaUi, required Future<T> Function() myself, Duration timeout})`（放在 `lib/cli/delegate.dart`，一处实现）

**为什么**：现在是「委派 → 等界面 90 秒 → 超时 → 报『界面没有回应』」，可视化
成了执行路径的一部分，一出问题就把 Agent 挡住。改成**首选路径 + 秒级兜底**，
而且**界面不在这条任务上就根本不走委派**——少了这一条，界面没开时每条写命令都要
白等 2 秒，47 镜挑下来就是一分半纯等待。

- [ ] **Step 1: 写失败的测试**

```dart
// test/cli/delegate_falls_back_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:ishkafel/cli/delegate.dart';
import 'package:ishkafel/core/storage/ui_where.dart';

/// 委派是**首选路径**（人看到的和落盘的同源），不是**必经之路**。
/// 界面不在场时它一秒都不该等——可视化坏掉的最坏后果只能是「这一步没看见」，
/// 不能是「Agent 卡住了」。
void main() {
  late Directory dataDir;
  setUp(() => dataDir = Directory.systemTemp.createTempSync('ishkafel_dlg'));
  tearDown(() => dataDir.deleteSync(recursive: true));

  test('界面不在这条任务上：零等待，直接自己干', () async {
    var triedUi = false;
    final sw = Stopwatch()..start();
    final got = await delegateOrDoItYourself<String>(
      dataDir: dataDir,
      taskId: 't_3',
      viaUi: () async {
        triedUi = true;
        return null;
      },
      myself: () async => '自己干的',
    );
    sw.stop();

    expect(got, '自己干的');
    expect(triedUi, isFalse, reason: '界面都不在，没有委派的理由');
    expect(sw.elapsed, lessThan(const Duration(milliseconds: 500)),
        reason: '界面没开时每条写命令白等 2 秒，47 镜就是一分半');
  });

  test('界面就在这条任务上：走委派', () async {
    writeUiWhere(dataDir, module: 'workbench', taskId: 't_3');
    final got = await delegateOrDoItYourself<String>(
      dataDir: dataDir,
      taskId: 't_3',
      viaUi: () async => '界面干的',
      myself: () async => '自己干的',
    );
    expect(got, '界面干的');
  });

  test('界面在，但没应：兜底自己干，绝不报失败', () async {
    writeUiWhere(dataDir, module: 'workbench', taskId: 't_3');
    final got = await delegateOrDoItYourself<String>(
      dataDir: dataDir,
      taskId: 't_3',
      viaUi: () async => null,      // 界面没接单
      myself: () async => '自己干的',
    );
    expect(got, '自己干的');
  });

  test('界面在别的任务上：也不等它', () async {
    writeUiWhere(dataDir, module: 'workbench', taskId: 't_10');
    var triedUi = false;
    final got = await delegateOrDoItYourself<String>(
      dataDir: dataDir,
      taskId: 't_3',
      viaUi: () async {
        triedUi = true;
        return null;
      },
      myself: () async => '自己干的',
    );
    expect(triedUi, isFalse);
    expect(got, '自己干的');
  });
}
```

- [ ] **Step 2: 跑，确认失败**

Run: `flutter test test/cli/delegate_falls_back_test.dart`
Expected: FAIL —— `delegate.dart` 不存在

- [ ] **Step 3: 写实现**

```dart
// lib/cli/delegate.dart
import 'dart:io';

import '../core/storage/ui_where.dart';

/// **委派是首选路径，不是必经之路。**
///
/// 「界面在场时写操作委派给界面」这条原则保留——它保证人看到的和落盘的同源，
/// 这个项目在「同一件事两处算」上栽过不止三次。改的是它的地位：
///
/// 以前是「委派 → 等界面 90 秒 → 超时 → 报『界面没有回应』」，于是可视化
/// 成了执行路径的一部分，一出问题就把 Agent 挡住。因果是反的。
///
/// 现在：
/// 1. **先读一眼界面在哪**。不在这条任务上就根本不委派，直接自己干、零等待
/// 2. 走委派的，[timeout] 是秒级（默认 2 秒），不是 90 秒
/// 3. 没应就自己干，**绝不返回失败**
///
/// 第 1 条不能省：少了它，界面没开的时候每条写命令都要白等 2 秒——47 镜挑
/// 下来就是一分半纯等待。**委派只在人确实看着的时候才有意义。**
Future<T> delegateOrDoItYourself<T>({
  required Directory dataDir,
  required String? taskId,

  /// 请界面代办。它没接单、或者干不了就返回 null
  required Future<T?> Function() viaUi,

  /// 自己干。**这条路永远存在，也永远成功或按真实原因失败**
  required Future<T> Function() myself,
  Duration timeout = const Duration(seconds: 2),
}) async {
  final onScene = taskId != null &&
      readUiWhere(dataDir)?.isOnTask(taskId) == true;
  if (!onScene) return myself();

  try {
    final viaUiResult = await viaUi().timeout(timeout, onTimeout: () => null);
    if (viaUiResult != null) return viaUiResult;
  } catch (_) {
    // 委派这条路出任何岔子都不该拖累正事——自己干就是了
  }
  return myself();
}
```

然后把三个调用点改掉：

- `ui_command.dart` 的 `_openTaskPage` / `_backToTaskList` / `new-task`：
  `waitForUi` 从 90 秒改成 2 秒；**超时不再返回 `exitEnv`**，改成
  「界面没跟上，我自己建/自己去了」并照常返回 0 加上 `"landed": false`。
  `ui new-task` 的兜底是直接调现成的 `script new` / `blank create` 建任务逻辑。
- `apply_command.dart:552` 那处 90 秒等待：改走 `delegateOrDoItYourself`。
- `review_command.dart` 同理。

**同时删掉** `ui_command.dart` 里 `next` / `note` 中那条 `ui tasks` 往返：

```dart
// 删掉这两行
'note': '界面正停在这条任务上，它占着写锁。接下来要写这条任务的话…先让界面退回列表：ishkafel ui tasks',
'next': switch (kind) { 'script' => 'ishkafel ui tasks && ishkafel script extract $newId <参考片>', ... }
// 改成
'next': switch (kind) { 'script' => 'ishkafel script extract $newId <参考片>', ... }
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `flutter test test/cli/delegate_falls_back_test.dart test/cli/ui_command_test.dart test/cli/export_delegate_test.dart`
Expected: PASS（旧测试里断言「界面没有回应 → exitEnv」的那几条要跟着改掉，
并在提交信息里说明为什么——那个行为本身就是这次要清零的东西）

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "fix: 委派降级成首选路径 + 秒级兜底

以前是委派→等界面 90 秒→超时→报「界面没有回应」，可视化成了执行路径的
一部分，一出问题就把 Agent 挡住。现在先读一眼界面在不在这条任务上，不在
就零等待自己干；走委派的 2 秒兜底，绝不返回失败。

顺带删掉 ui new-task 的 next 里那条 ui tasks 往返——lock.yield 上线之后
它就多余了，Agent 照着走等于白跑一个来回。"
```

---

### Task 9: 删掉 13 处 `exitLocked` 与锁本身

**Files:**
- Delete: `lib/core/storage/task_lock.dart`、`lib/cli/lock_yield.dart`、
  `lib/cli/gui_lock_guidance.dart`、`lib/cli/agent_lock_holder.dart`
- Delete: `test/cli/agent_lock_holder_test.dart`、`test/cli/gui_lock_guidance_test.dart`、
  `test/cli/lock_waits_instead_of_bouncing_test.dart`、
  `test/architecture/agent_has_unlock_path_test.dart`
- Modify: `lib/cli/cli_output.dart`（删 `exitLocked`）、13 个命令文件
- Modify: `lib/core/storage/ui_action.dart`（删 `UiAction.lockYield`）
- Test: `test/architecture/no_permission_refusals_test.dart`（新）

**Interfaces:**
- Consumes: 无
- Produces: 无

- [ ] **Step 1: 先写那条守门的架构测试**

```dart
// test/architecture/no_permission_refusals_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';

/// **软件永远不对 Agent 说「不行」。**
///
/// 产品负责人的原话：「永远不要让 Agent 报一个问题说『我现在没有办法操作，
/// 因为软件那边没有给我权限』……任何它不应该做的事情，都应该是人告诉 Agent 的，
/// 而非是软件限制的。」
///
/// 命令失败的理由只剩三类：参数不对（exitBadUsage / exitNotFound）、
/// 外部依赖真的坏了（exitEnv）、干了没成（exitFailed）。**没有第四类。**
void main() {
  test('exitLocked 这个退出码不存在了', () {
    final src = File('lib/cli/cli_output.dart').readAsStringSync();
    expect(src.contains('exitLocked'), isFalse,
        reason: '「被别人占着」不是一种允许的失败理由，整个退出码要删掉');
  });

  test('没有任何地方还在 import 锁', () {
    final offenders = <String>[];
    for (final f in Directory('lib')
        .listSync(recursive: true)
        .whereType<File>()
        .where((f) => f.path.endsWith('.dart'))) {
      final src = f.readAsStringSync();
      if (RegExp(r"import .*(task_lock|lock_yield|gui_lock_guidance|"
              r"agent_lock_holder)\.dart")
          .hasMatch(src)) {
        offenders.add(f.path.replaceFirst('${Directory.current.path}/', ''));
      }
    }
    expect(offenders, isEmpty,
        reason: '锁整套删掉了，这些地方还引着：\n${offenders.join('\n')}');
  });

  test('CLI 的失败话术里没有「权限类」的说辞', () {
    const banned = ['正在操作这个任务', '写不进去', '先等它', '被锁住',
      '界面没有回应', '占着，先不动它了'];
    final offenders = <String>[];
    for (final f in Directory('lib/cli')
        .listSync(recursive: true)
        .whereType<File>()
        .where((f) => f.path.endsWith('.dart'))) {
      final src = f.readAsStringSync();
      for (final phrase in banned) {
        if (!src.contains(phrase)) continue;
        // 注释里回顾历史可以，真往 stderr 写就不行
        for (final line in src.split('\n')) {
          if (!line.contains(phrase)) continue;
          if (line.trimLeft().startsWith('//')) continue;
          if (line.trimLeft().startsWith('///')) continue;
          offenders.add('${f.uri.pathSegments.last}：${line.trim()}');
        }
      }
    }
    expect(offenders, isEmpty,
        reason: '这些话是软件在对 Agent 说「不行」，一条都不许留：\n'
            '${offenders.toSet().join('\n')}');
  });
}
```

- [ ] **Step 2: 跑，确认失败**

Run: `flutter test test/architecture/no_permission_refusals_test.dart`
Expected: FAIL，三条全红

- [ ] **Step 3: 动刀**

1. 13 个命令里把

```dart
if (!lock.acquire(holder ?? agentLockHolder)) {
  sink.writeln(guiLockGuidance(lock.read()?.holder));
  return exitLocked;
}
```

整块删掉（连同后面对应的 `lock.release(...)` / `lock.heartbeat(...)`）。
`script_run_command.dart` 里那四处 `acquireYieldingFromUi` 同样整块删掉。

2. `lib/cli/cli_output.dart` 删掉 `exitLocked` 常量及注释。

3. `lib/core/storage/ui_action.dart` 删掉 `UiAction.lockYield` 枚举值及其
   长注释（那段注释讲的是「可视模式的死结」，而死结随着锁一起消失了）。

4. 删四个文件 + 四个对应的测试文件。

- [ ] **Step 4: 编译 + 架构测试转绿**

```bash
flutter analyze
flutter test test/architecture/no_permission_refusals_test.dart
```
Expected: analyze 无错误；架构测试 PASS

- [ ] **Step 5: 全量跑**

Run: `flutter test`
Expected: 全绿（`ui_action_not_wedged_test.dart` 里如果点名了 `lockYield`，
一并改掉）

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: 删掉锁——软件不再对 Agent 说「不行」

exitLocked 整个退出码作废，13 处调用点清零，task_lock / lock_yield /
gui_lock_guidance / agent_lock_holder 四个文件删除，UiAction.lockYield 删除。

命令失败的理由只剩三类：参数不对、外部依赖坏了、干了没成。没有第四类。
加架构测试守住这条，连失败话术里的「正在操作这个任务」「界面没有回应」
一起扫掉。"
```

---

### Task 10: 界面这边把锁去掉

**Files:**
- Delete: `lib/features/workbench/task_lock_banner.dart`
- Modify: `lib/features/director/director_page.dart:1325`、
  `lib/features/workbench/workbench_page.dart:234`、
  `lib/features/review/review_page.dart:256`
- Test: 沿用 `test/architecture/no_permission_refusals_test.dart`（扩到 `lib/features`）

**Interfaces:**
- Consumes: 无
- Produces: 无

界面打开一个任务不再需要「取得」什么，它就是打开。三处 `lock.acquire` 与随之而来
的只读态、橙色横幅一并去掉。

**注意**：横幅上那句「它此刻在做什么」不要连坐删掉——那是 `AgentPresence` 给的，
是可视化的一部分，人要知道 Agent 在动什么。只删「当前为只读」「强制接管」那一套。

- [ ] **Step 1: 把架构测试扫描范围从 `lib/cli` 扩到整个 `lib`**

```dart
    // no_permission_refusals_test.dart，第三个 test 里
    for (final f in Directory('lib')      // ← 原来是 lib/cli
```

并把禁用话术补上界面那几句：

```dart
    const banned = [..., '当前为只读', '强制接管', '正在操作这个任务，当前为只读'];
```

- [ ] **Step 2: 跑，确认失败**

Run: `flutter test test/architecture/no_permission_refusals_test.dart`
Expected: FAIL，列出界面那几处

- [ ] **Step 3: 动刀**

三个页面各自：删掉 `lock.acquire` 那一段、删掉 `_readOnly` 之类的状态与它对
编辑控件的禁用、删掉 `TaskLockBanner` 的引用，最后删文件。

播报横条（`AgentBroadcastBar` / `AgentStageOverlay`）**原样保留**。

- [ ] **Step 4: 架构测试转绿 + 全量跑**

```bash
flutter analyze
flutter test
```
Expected: 全绿

- [ ] **Step 5: 真机验那个最要紧的场景**

> 这一步是这次改造的验收正身，不许省。**别在真实数据上做自动点击**，
> 自己新建两条测试任务。

```bash
./scripts/pack.sh            # 出正式包，不要 debug
# 起 app，建两条空白任务（记下 seq，假设是 #A #B）
# 把界面停在 #B
ishkafel unit tags <#A> --unit 0 --tags 促单       # 界面在别的任务上，Agent 写 #A
ishkafel ui open <#A>                              # 把界面叫到 #A
ishkafel unit tags <#A> --unit 0 --tags 痛点       # 人正开着 #A 看着，Agent 写它
# 人手动在界面上改一处标签
ishkafel unit tags <#A> --unit 1 --tags 促单       # 人刚改完，Agent 接着写
ishkafel log <#A>                                  # 四笔都在，人和 Agent 分得清
```

Expected：
- 四条命令**全部零等待成功**，stderr 里没有任何一句「没有权限 / 被占着 /
  界面没有回应」
- `ishkafel log` 里看得到人手改的那一笔，`by` 是 `human`
- 人手改的那处标签**没有被 Agent 抹掉**

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: 界面这边把锁也去掉

打开一个任务不再需要「取得」什么，它就是打开。三处 lock.acquire、只读态、
橙色横幅、TaskLockBanner 全部删掉。

播报横条原样保留——那是 AgentPresence 给的，人要知道 Agent 在动什么，
跟锁没关系。"
```

---

### Task 11: 收尾——手册、更新说明、正式包

**Files:**
- Modify: `docs/AGENT_SKILL.md`
- Modify: `CHANGELOG.md`
- Modify: `docs/superpowers/specs/2026-09-17-agent全权与操作日志-design.md`（状态行）

- [ ] **Step 1: 手册里删掉所有教 Agent 绕锁的话**

搜这几个词并逐条处理：`ui tasks`（作为解锁出路的那几处说明）、`写锁`、
`让位`、`占着`、`强制接管`。`ui tasks` 本身保留，但改成「把界面支开、退回
任务列表」这一条普通导航说明。

- [ ] **Step 2: 手册里加一段「撞见人改过的东西怎么办」**

**给事实和出路，不给规则**：

```markdown
## 人也在动这条任务

你和人可以同时改同一条任务，软件不拦任何一方。所以：

- `task --json` 里每一处带 `editedBy`：这一处是谁定的、什么时候定的
- `ishkafel log <任务> --since <游标>` 给你这中间发生过什么

**撞见「这一处是人改的」之后怎么办，软件不管。** 绕开、照改、还是先跟人说
一声，你自己判断。要改的话把理由播出来（人在旁边看得见），别闷头覆盖。
```

- [ ] **Step 3: 跑一遍手册相关的架构测试**

Run: `flutter test test/architecture/skill_field_names_real_test.dart test/architecture/usage_lines_runnable_test.dart test/architecture/no_retired_words_test.dart`
Expected: PASS（`no_retired_words` 里要把「写锁」「强制接管」加进退役词表）

- [ ] **Step 4: 全量 + 打正式包**

```bash
flutter analyze
flutter test
./scripts/pack.sh
```

> 留给用户的必须是正式构建。调试版不带云端 AI 凭据和内置 CLI，
> 用户接手会看到「未配置」的降级提示并以为软件坏了（真机发生过三次）。

- [ ] **Step 5: 把设计文档的状态改掉**

```markdown
**状态**：期一~期三已落地（<日期>）；期四、期五待做
```

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "docs: 手册删掉绕锁的说明，加上「人也在动这条任务」

给事实和出路，不给规则：editedBy 说这一处是谁定的、log 说这中间发生了什么，
绕开还是照改由 Agent 判断。"
```

---

## 做完这一批能验什么

对照设计文档「怎么验收」那一节的前两条：

1. **全程不出现任何一句「没有权限 / 被占着 / 界面没回应」**——Task 9、10 的架构
   测试 + Task 10 Step 5 的真机四连
2. **中途人为制造干扰**：Agent 干到一半，人把界面翻到别的任务、手动删掉两个分镜、
   再翻回来。Agent 接着干不卡，而且读完 `ishkafel log` 能说出人删了哪两个、
   为什么它认为人不想要那一类

后两条（人翻走了 Agent 下一条命令就知道 / 画面不弹回第一行）要等期四、期五，
各自另出计划。
