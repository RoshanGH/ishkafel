# 更新记录

面向使用者写：说清**这一版能多干什么、少踩什么坑**，不复制提交信息。
版本号与「设置 → 关于」里显示的一致——同事发截图过来时，那是唯一能对齐的锚点。

---

## 0.1.118

- 命令行也能清理残留产物了（`ishkafel clean`）。以前只有设置页能点，
  而 Agent 跑久了攒下的几个 G 全是它自己造的

## 0.1.117

- 替换裂变的配乐，命令行也能铺了（`ishkafel bgm`）：铺一段、改音量、删掉。
  以前只有界面能做，而配乐是进成片的东西

## 0.1.116

- 几处提示只告诉人「点某个按钮」，而用命令行的人没有按钮可点——
  现在两条路都给出来

## 0.1.115

- 替换裂变的换音色，命令行也能做了（`ishkafel voice`）。以前只有界面能换，
  而导出会拦下「选了音色却没生成配音」——等于 Agent 被那道拦截堵死

## 0.1.114

- 挑素材时查时长快了近 4 倍（一页 50 条从 72 秒降到 19 秒）：以前一条条
  串着量，现在并行

## 0.1.113

- 挑素材时会告诉你「选它之后画面怎么放」：多数是 1.0 倍（软件从素材里
  截一段用），以及这一段可以在素材的哪个区间里挪

## 0.1.112

- 修：命令跑到一半被中断后，它占的锁要等一分钟才失效。锁上没记进程号，
  「写锁的进程还在不在」这条快捷判断对命令行完全无效——现在记上了

## 0.1.111

- 快了 15 倍：提交方案时量素材时长，以前每条都去联网量（102 条要一百多秒），
  现在素材已经下到本地的直接读本地，同样一批 7 秒跑完
- 顺带更稳：网断或地址过期时不再连时长一起丢——丢了那几镜会退回快进

## 0.1.110

- 修：软件里预览短镜头还是快进的，而导出来的已经不是了——预览没跟上
  「从素材里截一段」的改动。现在预览、导出、剪映三处口径一致

## 0.1.109

- 修：导出的 mp4 已经不快进了，拖进剪映的工程却还是三十几倍快放——
  剪映那条路没跟上「从素材里截一段」的改动，两边口径不一致

## 0.1.108

- 提交方案时会说清「有几条素材量不出时长」以及后果——那些镜头会退回
  整条压缩、画面明显快放。以前是悄悄降级，人拿到片子才发现有几镜在快进

## 0.1.107

- 修：删掉脚本成片任务时，导出中间产物、预览切片、镜头首帧这些不会被清掉，
  在盘上越积越多（真机上量到近 900M 没人认领的文件）

## 0.1.106

- `ishkafel task` 现在会报已选素材的时长，Agent 能确认「短镜头有没有被截一段」
  这件事真的生效了（以前只能盲猜）

## 0.1.105

- **修：Agent 提交的方案，短镜头照样是快进**。上一版做的「从素材里截一段」
  要靠素材时长才算得出来，而那个数只有在界面上挑素材时才会存下来——
  Agent 走命令行提交的方案根本没有，取段全部失效。现在提交时一并收下来
- 修：Agent 新建任务后，底部播报一直停在「正在新建任务」不消失

## 0.1.104

- 内部清理：去掉改版留下的死代码
- 修：新建任务的模式参数填错时，报错还在教人用已经废弃的写法

## 0.1.103

- 修：Agent 干活的播报还是不显示。三个地方各定各的节奏——播报条要求每步停
  半秒才继续，编导台和工作台等 0.32 秒就放行了，于是播报整步被跳过。
  现在全软件用同一个节奏

## 0.1.102

- 可视化：Agent 干活的过程现在会一步步说出来——在看参考画面、按什么找、
  找到多少条、量完时长在挑。以前只在开头报一句，人看到的是「跳过去→结束」，
  中间几秒全黑
- 界面没开着时不再一步步干等回执（一条命令报五步会白耗二十几秒）

## 0.1.101

- 修：Agent 干活时底部的播报条经常一片空白。它扫在场状态时把回执、
  请求结果这些同目录的旁支文件也当成了任务，撞上一个就把真正在干活的挡住了

## 0.1.100

- 修：脚本成片的「找镜头」带了 `--visual` 也没反应——软件不弹出来、界面也不跟。
  那条命令收了参数却从来没上报过自己在干什么
- 加了一道检查：以后再有命令收了 `--visual` 却不上报，测试会拦住

## 0.1.99

- **可视化模式：界面开始跟着 Agent 走了**。以前它说在挑 U3S2 的素材，
  界面只把播放头挪一下，别的什么都不动；现在会选中那一镜、右栏切到
  「替换素材」——跟人自己点过去时走的是同一条路
- `ishkafel candidates --visual`：挑素材这一步终于看得见了。
  这和提交方案是仅有的两个真正在做决策的地方，以前都是黑箱

## 0.1.98

- 替换裂变现在能改字幕样式了（工作台顶部「字幕」按钮，命令行 `ishkafel subtitle`）。
  以前这条线写死用白字黑描边，人和 Agent 都没得选
- **遇到画面自带烧录字幕的素材有救了**：切成底条或毛玻璃能盖住。
  以前默认样式盖不住，原字幕从描边缝里透出来，成片上两行字打架

## 0.1.97

- **短镜头不再被拉成快进**。以前是把整条素材压缩进坑位——原片的快切镜头
  只有 0.4~1 秒，而素材库里的分镜普遍 4~30 秒，于是这些镜头全是 10~30 倍
  快放（一条 34 镜的片子里将近四成中招）。现在改成**从素材里截一段用**，
  倍速回到 1.0；素材开头常有转场，默认取中段
- 提交方案时可以指定从素材哪儿开始截（`trimStarts`），不指定就自动

## 0.1.96

- 修：命令行的「导剪映」遇到没下载的素材直接放弃，而导出成片是会自己下的。
  现在它也自己把素材备齐再写工程
- Agent 能自己验片了：`peek` 抽帧到本地（看候选、看成片），
  `candidates --probe` 给出「选这条会变速多少」

## 0.1.95

- 剪映工程生成完的对话框补上「打开剪映」（编导台一直有，工作台漏了）
- 生成过程有进度了：一百多个素材要归集到工程目录，以前界面干卡着不说话
- 素材没落到本地时不再让它跑到一半才失败——和导出成片用同一把闸

## 0.1.94

- **替换裂变可以导出到剪映了**（工作台底部「剪映」按钮，命令行 `ishkafel jianying`）。
  一个任务一份工程：同一个位置挑了几条候选就摞几条轨，上面那条盖着下面的——
  想用别的候选，在剪映里把上面那条关掉就行。等于把「挑哪一条」搬进剪映边看边切
- 工程里的轨道：分子轨（原片按台词语义单元切）、原子轨（原片按视觉镜头切）、
  候选轨若干、配乐轨、字幕轨。字幕是文本不是图，能直接改字

## 0.1.93

- 新建任务改成两步：先选**替换裂变**还是**脚本成片**，选完替换裂变才问
  「有原片还是不用原片」。以前四张卡并列摆着，看起来像四种玩法，
  其实本地文件和「不用原片从素材拼」是同一条线的两种起点
- Agent 那边同步：`ui new-task --mode` 的 `renew` 改叫 `replace`（旧名仍然认）

## 0.1.91

- 统一用语：「成片翻新」改叫**替换裂变**。两条线各有两种起点，起点不是模式——
  替换裂变可以从原片开始，也可以不用原片直接从素材拼；脚本成片可以从参考片
  提取台词，也可以直接写。文档、界面文案、Agent 手册与技能描述一起对齐

## 0.1.90

- 修：重启 app 后打开任务，顶上总有一条「这个任务被另一个窗口占着，当前为只读」，
  要么干等一分钟要么强制接管。写锁的那个 app 早就退了——现在进程不在了立刻放行

## 0.1.89

- 修：每次打开任务都闪一句「正在把 N 条已选素材存到本地」。素材其实没重下，
  是每次都把它们重排一遍队列——现在本地已有的直接算就绪，开任务就能用

## 0.1.88

- 候选选多了不再拦着不让导出。以前底部横幅写「无法导出，请先减少 U2 选中的
  候选素材」，而 U2 的因子有四百多万——那等于让人从头再挑一遍。
  现在照选，导出时由软件挑出 100 条
- 这 100 条挑得更有意义了：从候选最多的那个单元开始变，
  以前它夹在中间的话一百条里它一动不动

## 0.1.87

- 修：分析完成的任务仍挂着红色「上次分析被中断，请重新分析」。
  分析成功时没人清掉上一次的错误标记——点「重试」会白花一次分析的钱。
  已有的这种任务下次启动自动清掉

## 0.1.86

- 挑素材面板：标签命中太多时自动改用画面描述检索，并在提示行说明原因
  （以前人在界面上拿到的也是「最新 50 条」而不是「最像的 50 条」）
- 挑素材：可以排除指定项目的素材——翻新常要「换成别的项目拍的」，
  而搜出来最像的永远是原项目自己拍的
- 导出的文件名改用方案名（`A-居家写实线.mp4`），不再是「变体0/1/2」

## 0.1.85

- **修：导出必然失败**。字幕图渲染在并行的段落之间互相覆盖清单，导致
  「某某句没有产出图片」——三条方案一条都出不来。现在同一目录上的渲染排队进行
- 导出全部失败时最后一行不再写「完成」，改成「全部失败，一条都没出来」；
  失败按错误级别记，不再混在 warn 里
- 挑素材：按标签命中太多时自动改用画面描述语义检索并说明原因。
  素材库按标签搜是「最新 50 条」而不是「最像的 50 条」，几万条里取 50
  等于随机取样（真机上同一镜从「户外街道」变成了对得上的「书桌前长痘女孩」）

## 0.1.84

- 修：Agent 用的命令行工具拿不到 AI 凭据——人在界面上能分析，Agent 敲命令却在
  第二步就失败。现在正式包会把凭据一起带上，拿到包直接就能用
- 新增 `ishkafel doctor`：开工前一次问完凭据、素材库登录、ffmpeg，缺什么点名说
  （以前要等干到一半才暴露）

## 0.1.82

- 修：素材库里 id 是负数的分镜取不到详情，会显示「落地失败」
- Agent 用的命令行工具现在自己知道 app 装在哪，app 关着也能把它拉起来
- 说明书里「翻新还是重做」的选法改了：看的是原片的声音还要不要，不是要出几条
- 修：命令行工具因为引到了界面代码而编不出来（编译器直接崩，报错里连文件名都没有），
  现在有测试守着这条线

## 0.1.78

### Agent 干活时，底部会一句句播报它在做什么

以前只有一条横幅写着「Agent 正在操作这个任务」，下一步一到就被覆盖 ——
切太快等于没有。

现在底部浮出一条毛玻璃播报，**保留最近 5 条**：

```
✓ 正在新建任务
✓ 正在提取台词
✓ 正在定本片的音色与语速
✓ 正在给第 3 行挑镜头
▸ 正在给第 3~15 行铺配乐
```

最新的一条在最下（视线自然落在底部），往上依次变淡；做完的打勾变灰，
只有正在做的那条亮着 —— 一眼看出走到哪儿了。

**每条至少停 0.5 秒**你才看得清。这个节流放在界面这头：界面显示够时间了
才回执，Agent 收到回执才走下一步 —— 所以是界面在控节奏，不是让 Agent 空等。

播报文案也改了，现在会说清**对哪几行做了什么**：以前是「正在铺配乐」，
现在是「正在给第 3~15 行铺配乐」。

它跟着 Agent 跨模块走（新建任务 → 编导台 → 找镜头 → 导出），切页面不会断。
不挡点击 —— 你随时能接手。

## 0.1.77

### Agent 现在看得见「它自己挑的那一镜」

以前给 Agent 的已选镜头**全是数字和文字** —— 素材 id、名字、时长、倍速，
没有一样是能看的。配音更彻底：连音频文件在哪都不告诉它。

而人在界面上是**看着画面挑**的。

新增 `ishkafel script frames <任务> --line N`，一次拿到这一行能看能听的
全部：参考镜长什么样、**自己挑的那一镜长什么样**、配音在哪。

有个细节：已选镜头抽的是**成片里真正会出现的那一帧**（取段之后），
不是素材开头 —— 给错了帧，Agent 看到的就不是成片里的东西，跟你讨论时对不上。

### 你说「第三行这个分镜不对」的时候

手册里现在写清了该怎么做：**别急着换个词重搜**，先把「要复刻的目标」和
「现在的结果」两张图都打开看，找出差在哪，再问你一句「是嫌画面太暗，
还是这个动作不对」—— 拿着图跟你确认方向再动手。

瞎猜着重搜的代价很实在：上一轮实测连搜三轮，回来的全是别的品牌。

### 修：检索会跨品牌搜回别家的素材

任务明明选了「AI自然消毒液原子库」，搜出来却混进防晒乳和洗护。

两道约束同时失效了：项目是空的，而参考镜没打标所以标签也是空的 ——
**旧代码遇到空标签就直接放弃全部约束**，等于在全库里捞。

现在没打标时会退回用**任务标签组的整个词表**收窄；两道都没有的话，
会明确告诉你「这一轮是在全库里搜的」，而不是让你以为搜到的就是对的。

## 0.1.76

### 补上一个让上一版新功能整个落空的缺口

上一版我做了「让 Agent 看见画面」—— 参考片每一镜带首帧图。**结果在最该用
它的场景里，那个字段是空的。**

原因：从参考片提台词（`extract`）只出台词和切点，**参考镜的画面描述、标签、
首帧图全是空的**，而这三样正是挑镜头的三条依据。软件里这一步是「你点开
某一镜时才去打标」（打标花钱），命令行这条路压根没有入口。

所以 Agent 只能自己抽帧、肉眼看图、手写关键词去搜 —— 中间多了一层转译，
写偏一点就搜回一堆别的品牌的素材（这次实际发生了三次，只能换词重搜）。

现在补了 `ishkafel script tag-ref <任务> --line N`，打完那一行才有描述、
标签和图。手册里也把它写进了必经路径。

### 配音断不了句的行，现在会被拦下来

有一句台词太短（「加一点。」），语音识别认不出来，那一行拿不到逐字时间 →
**断不了句 → 字幕整句糊在屏幕上**。而这种情况以前只记一条日志，导出照过。

这正是「导得出来但片子是坏的」那一类，现在进拦截清单，并告诉你怎么办
（重新生成那一句，或者把太短的句子改一改）。

### 「改了但显示没改」

定音色、调音量这类改动以前返回「应用了 0 条」—— 读起来像什么都没落盘，
其实是生效了的。现在会直接说改了什么：「本片基调：音色 vivi 2.0」。

## 0.1.75

### 端到端跑通了，但暴露出四个卡点

让一个只有说明书的 Agent 拿你那条滴露视频完整走了一遍 —— **成片出来了**
（95.5 秒、1080×1920），但中间两处让流程整个停下来。

**一、挑镜头时它提交的候选一律被判「只能出 0.0 秒」，21 行全废。**

原因：候选素材里没有时长。界面上是拿预览地址现探的，命令行这条路漏了这一步 ——
而手册又要求「把候选原样带回来提交」。**照着手册做必然全军覆没。**
现在候选会带上时长了。

**二、配乐整个断掉，连「这几行不铺配乐」都提交不了。**

取候选曲子的命令根本不存在（手册的样例里有，但没说从哪来）；而且不给曲子 id
时被当成「id = -1」去查候选，于是空段也被拒。所以那条成片是**没有配乐的**。
现在补了 `script bgm-candidates`，空段也放行了。

**三、新建任务的闪退 —— 前两次根因我都猜错了。**

验收数据把规律定死了：只有「本进程第一次打开编辑页」且走新建那条路时才崩；
同样是第一次，走 `open` 从不崩。差别在于新建那条路中间隔着一个**弹窗**，
它关闭时界面正在重建，这一刻去初始化播放器就撞上了。

现在播放器改成**等界面画完第一帧再创建**，两条路进来都是稳的。

**四、`--name` 上一版说做了、其实没接上线。**

我只跑了静态检查没验行为。这次补了测试，并且加了 `ishkafel task-rename`
（界面上人能改名，命令行之前不能）。

### 这一趟花了多少

云端只烧了三处：提台词的语音识别（75 秒）、配音合成（466 字 / 21 句）
外加 21 次逐字时间戳、素材语义检索 24 次。另外下载了 401MB 素材到本地。

导出走本地 ffmpeg，不花钱。

## 0.1.74

### Agent 现在能用上全部五种检索方式

你问的「软件支不支持首帧图去搜索」—— **支持，而且界面一直在用**，
但 Agent 之前只能用其中两种。

| 方式 | 靠什么找 |
|---|---|
| 标签 | 参考镜带过来的标签 |
| 画面描述 | 自己写一句画面去搜 |
| **以图搜图** | **某条素材的画面找同类** ← 之前 Agent 用不了 |
| 口播词 | 素材自己说了什么 |
| 素材名 | 同一批常有命名规律 |

**以图搜图是复刻场景的主力**：画面里很多东西用文字说不清楚，而 Agent
手里就有参考镜的图。现在它的路子是 —— 先按描述搜一轮、抽图看一眼挑出
最像的那条，再拿它去找一批同类。

认不出的检索方式会直接报错并列出可用值，不会默默退回某一种 ——
那样 Agent 会以为自己搜的是 A、实际搜的是 B，而候选看起来都像那么回事。

## 0.1.73

### Agent 现在能「看见」画面了

以前给 Agent 的候选素材只有一句画面描述和一个会过期的网址 —— 它读不了远程图，
等于**只能靠别人总结的二手信息挑镜头**。

现在：

- **参考片每一镜都带本地首帧图**（`framePath`）。Agent 打开就能看见「要复刻的
  是什么画面」，而不是只读一句描述
- 新增 **`ishkafel script peek <任务> --materials 105378,105361`** ——
  把拿不准的那几条候选的画面抽到本地，亲眼看看再决定

按需抽，不是每次都下：一条素材几十兆，先按描述和标签筛一轮，剩下的再看图。

手册里也把这一步的定位写清了：**挑镜头是 Agent 的活，不是软件的活。**
软件的「自动铺一版」只会照着描述去搜，它看不懂视频；Agent 看得懂 ——
参考镜的画面、标签、那一段在说什么，全都拿得到。

## 0.1.72

### 修：新建任务导致的闪退（这次找对根因了）

上一版我以为是「软件刚启动、播放器没准备好」，加了几秒缓冲 —— 判断错了。
崩溃报告显示三次崩溃时软件已经活了 **9 分钟 / 23 分钟 / 89 秒**，缓冲一次
都没用上。

真正的原因：`ui new-task` 建完任务后**直接进新任务，没有先退回列表页**。
上一个任务的编辑页还留在栈上活着，新的又开一个 —— 两个视频播放器同时存在，
底层直接崩掉。

对照很清楚：`ishkafel open` 那条路一直不崩，因为它**先退回列表页再进**。
现在新建也这么做了。

### 修：Agent 会被自己刚建的任务锁在门外

`ui new-task` 建完把界面停在新任务上，于是那个任务被界面占着，紧接着的第一条
写入就被拒 —— 而「占着的人」正是它自己刚叫出来的。更糟的是原来的提示写着
「**或在 app 里强制接管**」，等于让一个只有命令行的 Agent 去点界面。

现在会明确告诉它：把界面挪开就行（`ishkafel open <另一个任务>`），并且说清
「这个占着的人很可能就是你自己刚叫出来的」。

### 补：手册里写了、却根本不存在的命令

`ishkafel voices`（列可选音色）—— 手册一直让 Agent「别自己挑音色，用这条
命令列给人看」，而这条命令从来没有过。结果它既不能挑、也不能列，整条配音
链路在纯命令行下是死的。

现在补上了。**并且加了一道检查：手册里写的每条命令都必须真的存在**，
以后不会再出现这种事。

### 补：两个「给了参数却被默默吃掉」的地方

- `ui new-task --name` 现在真的能给任务命名了
- `script shots --keyword`：没有参考片时，可以自己给一句**画面描述**去搜候选。
  以前提示说「只能按台词与标签找」，但那条路压根没有对应参数

## 0.1.71

### 修：新建任务会让软件闪退

上一版你可能见过「ishkafel 意外退出，是否重新打开」——那不是巧合。

`ui new-task` 会先把软件拉起来，然后立刻下单。软件冷启动 1.5 秒就能接单，
接着弹向导、建任务、进编导台初始化播放器 —— 而那时播放器的底层还没准备好，
直接断言失败、整个软件 abort。崩溃报告里两次的栈一模一样。

更糟的是崩完之后：后面所有可视模式的命令都会报「打不开 app」，而手册说
这种错别重试 —— 人不在场就是死局。

现在两头都加了缓冲：软件启动后缓几秒才接单，命令那边探到是冷启动也会多等。

### 修：可视模式写台词，会把已经写好的回滚掉

上一版没修干净。这次是**按内容判定**：界面写盘前先比一眼盘上被谁动过没有，
对不上就不写。

上一版靠的是「Agent 在场时界面不写」，但「在场」是每半秒轮询出来的 ——
那个窗口里界面照写不误。所以会出现「写第二句时把第一句冲没了，而命令全程
报成功」。按内容判定不依赖时序。

### 修：左栏脚本列不跟着刷新

Agent 改了台词，右边的卡片变了、**左边那一列还是旧的**，同一个界面上同一行
显示着两个版本。原因是那个输入框只在「换了另一行」时才同步文本。

现在只要不是你正在打字的那一行，外部改动都会同步进来。

### `ui new-task` 会告诉你建的是哪一条

以前只回一句「任务已经建好了」，调用方得自己去列表里翻最后一条猜 ——
两个 Agent 同时建就猜错了。现在直接给 id、编号、名字和下一条命令。

## 0.1.70

### 三个被外部验收抓出来的 bug

开了另一个 Agent，只给它说明书、不给源码，让它照着手册从零走一遍。
它抓到三个我自己测不出来的问题：

**一、`ui new-task` 报失败，但任务其实建成了。** 建完之后代码在等你从
编导台退出来才发回执，于是命令必然等到超时。Agent 照着退出码会去重试 ——
再建一个垃圾任务。现在建完立刻回执。

**二、可视模式改台词会静默丢。** 命令返回「成功」，盘上没变。原因是界面
被唤醒打开时内存里是旧数据，之后它任何一次自动保存都把旧的整份盖回去。
**这比报失败更危险**：报失败你会重试，报成功你会带着错往下走。

现在 Agent 在场时界面一个字都不写，而且**会跟着 Agent 的改动实时刷新** ——
以前只做了「滚到那一行」，没做「显示新内容」，所以你看着的是一块不动的板子。

**三、编导台在 Agent 干活时是一张空白拦截页。** 审片台早就改成「只读跟随」
了，编导台漏了。验收 Agent 的原话：「人在旁边看着，看到的是一块黑板」。

### 补了几个 CLI 的洞

- **`ishkafel task-delete <id> --yes`** —— 以前根本没有删除任务的命令，
  Agent 只能看着界面上的 ⋯ 菜单不敢点
- 手册补上：`apply lines` 的三个 op 分别是什么（`insert` 是「在这一行**之后**
  插」，以前只文档了 `set`）、行的两种类型、三套下标基准的区别、
  `ISHKAFEL_APP` 到底怎么找

## 0.1.69

### 新建任务：Agent 当着你的面建

以前 Agent 建任务是在后台悄悄建好，界面一动不动 —— 你只能从「命令说成功了」
去猜它做对没有。现在有一条专门给可视模式用的命令：

```bash
ishkafel ui new-task --mode script --tag-groups 1261,1262
```

软件弹出来 → **新建向导真的打开** → 来源和标签组真的填上 → 停一拍让你看清 →
真的点「创建」→ 进任务。

**不是「演」出来的**：向导是真的那个向导，创建走的是你自己点创建时的同一段
代码。演一套写一套的话两边迟早对不上 —— 这个软件已经因为「同一个东西两处算」
栽过三次。

参数不合格时**不会弹向导**（比如 renew 没给原片、一个标签组都没选）——
让你看着窗口弹出来又关掉，比不弹更糟。

### 手册里明写了「不要用 Computer Use」

你那个验收标准 —— Codex 不该自己觉得这些事情要靠截图点击来做 —— 靠的就是
这句话，加上一份「界面动作对应哪条命令」的清单。

真遇到没有对应命令的界面操作，它应该**说出来让你补**，而不是绕过去自己点。

## 0.1.68

### 修：Agent 在外面建了任务，界面上看不见

你用 Codex 建了任务 5，界面一动不动 —— 因为**任务列表只在进页面那一刻
读过一次**。CLI 在外面建的任务、改完的任务，正开着的界面完全不知道，
你只能退出去重进。

这不是「可视模式才需要」的东西：静默模式下你回头来看，也该看得到。
现在列表会自己发现盘上的变化并刷新。

### Agent 手册重写了定位

以前开篇就说「这是个成片翻新工具」，脚本成片被当成附属拼在最后。
**它俩是平级的两条线**，而且脚本成片根本不需要原片。

Agent 读完开头就形成「这是翻新工具」的印象，从台词造新片那半边会被埋没。
技能描述也一样窄 —— 有人说「帮我做条口播视频」，它根本不会想到这个工具。

现在开篇先讲清一句话：**台词决定时间，画面填进去**。然后摆出两条线
和「怎么选」，还点出了容易混的旁支（空白任务是拼画面、没有配音字幕）。

## 0.1.67

### 划词建镜开放给 Agent 了

「人能干的，Agent 都要能干」——划词这条线之前它完全干不了：读不到镜头
绑了哪几个字、读不到每个字落在哪、也提交不了。

现在它能做和你一样的事：

```bash
ishkafel script show <任务> --line 3 --json | jq '{words, takenWords}'
ishkafel script apply word-shots <任务> --file w.json
```

规则和界面上完全一致：已占住的字不能再划、可以不连续、必须先有配音、
素材必须来自候选。重叠时会点名**该先删哪一镜**，而不是含糊拒绝。

### 顺带发现：两类改动从来没写进 Agent 手册

`apply alloc`（分镜头时长）和 `apply bgm`（铺配乐）——手册里只讲了规则、
**没给命令**，所以 Agent 一直不知道怎么提交。现在补上了，还带了示例。

### 加了一道防线

新增架构测试：**每一类 Agent 能提交的改动，都必须在手册里出现**，
否则测试直接红。

这个坑今天已经踩了三次（三轨混音台、脚本成片整条线的手册没有交付通道、
划词建镜）。靠人记得是靠不住的，所以让「漏同步」这件事本身变成测试失败。
这道防线一加上就立刻抓出了上面那两类。

## 0.1.66

### 修：播到某一镜时，右边整块变成一片灰

不是布局错乱 —— 那是 Flutter 在正式包里显示「这块渲染崩了」的样子。

崩的是**取段条**。你第 6 句第 2 镜的素材只有 3.5 秒，取段起点却是
19.9 秒（素材长度的 5 倍开外），取段条按这个数算出「窗口左边缘正好顶到
条尾」，接着一句 `clamp(8, 0)` 上下限颠倒，直接抛异常。

那个不可能的取段起点是怎么来的：**挑素材那一刻素材时长还没探测出来**，
边界夹不了；等时长回来，那个数就悬在素材之外没人再管。

两头都堵上了：

- 探测到素材时长时，顺手把悬空的取段起点夹回来（还会留够这一镜要用的
  那一段）。你现有的数据打开就会自愈
- 取段条的算术从界面代码里搬出来单独测 —— **数据再离谱也不许崩**。
  界面可以显示得难看，不能整块消失

这个坑一直都在，只是以前没人会手动展开那一镜；上一版加了「播到哪展开
哪」，就一头撞上了。

## 0.1.65

### 修：划完词再加分镜，划的词白划了

你昨天划完词又用「找镜头」加了两个分镜，结果四个镜头全变成 2220ms
（8880÷4）—— **划的词被平摊掉了，而且不报错、不提示**。

原因是「同一个东西两处算」：重排镜头时长的代码散在 9 个地方，划词建镜那
一处认识划词镜，「找镜头」那一处不认识，于是后者把整行重新平摊了一遍。

这一版把它收敛成**唯一入口**，调用方不需要知道这一行有没有划词镜。9 处
全部换掉，老函数在 lib 里已经零引用。

你那一行不用手动修：新版本里任何一次触碰（加镜、删镜、改时长）都会自动
重算回 1280 / 2040 / 2780 / 2780。

### 修：划过词的那一行，点它预览不跳了

其他行都正常，只有正在划词的那一行点了没反应 —— 因为可选文本会把点击
吃掉，不冒泡到行卡片。现在点台词也算点这一行。

## 0.1.64

### 划词之后，字幕自动跟着画面切

这是划词的另一半：**一次操作同时定下画面边界和字幕边界**。

- 划词的镜头把边界让给字幕，画面切到哪儿字幕就换到哪儿
- 中间的自由段自成一屏 —— 那里放了几个镜头是画面的事，字幕不必跟着碎
- 一屏字太多照样再细分（可读性是硬要求），但细分点落在划词边界之内
- **你手动改过字幕的行不受影响**：改过就是改过，之后镜头怎么变都不再
  自动重切。画面说画面的、字幕说字幕的，这条一直都在

### 字幕盖不住语音时会被拦下来

你那条片子的第 3 行就是这个问题：

> 第 1 屏字幕只有 8 个字，但这一屏要念 26 个字、4.7 秒 ——
> 中间那段配音在响，字幕却停着不动。

现在这种情况会进 `blocking`，**导出前必须处理**。不是拦着你不让写短字幕，
是不让它悄悄漏到成片里 —— 你之前就是拿到片子才可能发现。

判据刻意宽松（少一半以上才报）：同音改写、去掉语气词、把「一百块」写成
「100 块」都是正常操作，不该被啰嗦。在你现有数据上跑，正好只抓出那一处。

## 0.1.63

### 划词建镜：选中几个字，画面时长自动对上

以前一句话过三个分镜，你得自己听、自己数「如果你觉得有点贵」读了几秒，
再手动把镜头时长调成那个秒数。现在不用了：

**在选中的那一行台词上划中几个字 → 「用这几个字加分镜」→ 挑画面。**
这一镜的时长就是这几个字的朗读时长。

- 素材 5 秒、这几个字只要 2 秒？照旧走切或变速，一个字都不用改
- 划过的字带底色，**再划不动了** —— 想改就删掉那一镜，字自动恢复可划
- 划头、划尾，中间那段留给自由镜：8 秒的句子划掉头尾各 2 秒，
  中间不管放几个镜头，加起来永远是 4 秒
- 划词的镜头按字序排位，**先划句尾再划句首也不会乱**

### 顺带修好了一个你看不见的偏差

切点从「最后一个字收音」改成「下一个字开口」——**标点的气口归前一镜**。

你那条 43 字的长句里，「贵」收音在 1440ms、下一个字「那」开口在 1560ms，
中间 120ms 是逗号的气口。按收音切，画面在停顿刚开始时就切走，急了小半拍；
按开口切，画面正好在下一句出声那一刻换。全句 5 个标点处的气口是
80~280ms，都在眼睛看得出来的量级。

句尾那 200~350ms 余韵也归最后一镜，所以整句的镜头加起来正好等于配音长度，
段与段首尾相接、中间不漏一帧。

### 改台词会清掉划词的分镜（清之前问你）

台词一改，字的位置全变，「这几个字配这个画面」就不成立了。所以要清 ——
但清之前会告诉你损失什么：「这一行有 3 个分镜是按字划出来的（其中 2 个
已经挑好素材）」，你可以反悔。

## 0.1.62

### 派生产物也按项目存了，共享目录彻底清空

0.1.61 把素材和配乐按项目存了，还剩两个跨任务共享的目录 ——
**预览代理**和**人声分离结果**。它们是算得出来的，但一样没人收：任务删了
还躺在盘上，而且按内容指纹命名，根本反查不出归谁。

现在全部跟着项目走：

```
materials/<任务id>/   素材
bgm/<任务id>/         配乐
voices/<任务id>/      配音
proxy/<任务id>/       预览代理
vocals/<任务id>/      人声分离结果
```

**共享目录只剩 `separator_models/`** —— 那是人声分离的模型，是工具不是
任务数据，本来就该共享。

### 第一次打开这一版会清掉旧的预览缓存

老的 `preview_proxy/`（约 104 MB）和 `material_vocals/`（约 15 MB）会被
整个清掉 —— 它们按内容指纹命名，没法分发到各个任务名下。

**这两样会自动重建**，不会丢东西，但要花时间：预览代理是首次播放时重转；
人声分离比较贵（要跑模型，分钟级），只有「整段替换 + 铺了配乐」的段落
才会用到。

### 顺带：清理逻辑简单了一大截

以前预览代理判不了归属，只能按 10 GB 配额硬砍；人声分离得拿「现存任务还
引用哪些素材」反推孤儿。**这两套机制现在都删掉了** —— 东西在任务名下，
删任务直接带走。

## 0.1.61

### 物料改成按项目存，不再有孤儿

以前素材和配乐存在**跨任务共享**的缓存里（`material_cache/`、`bgm_cache/`），
按素材 id 命名。好处是同一条素材用在几个片子里只下一次；代价是**任务删了
没人收** —— 盘上永远躺着一批不知道归谁的文件，谁都不敢删。

现在它们跟着项目走：

```
materials/<任务id>/…   这个片子用到的素材
bgm/<任务id>/…         这个片子用到的配乐
voices/<任务id>/…      配音（本来就是按项目的）
```

**删项目就一起删干净。** 同一条素材用在两个片子里会存两份 —— 这是「删得
干净」的代价。

### 启动时自动归位，并清掉历史孤儿

第一次打开这一版，会把老的共享缓存分发到各个任务名下，然后收掉共享目录。

在你的数据上演练过：**66 个素材里 53 个有人用，13 个是孤儿（88 MB）**——
那些是以前挑过又换掉的素材，一直没人清。配乐 2 个都在用。

迁移有三条保命规矩：
- 已经在目标位置的文件**不覆盖**（上次迁到一半被杀，重来时不会被半截文件盖掉）
- 每个用到它的任务都拿到副本，**才删源文件**；有一个没成功就留着下次再迁
- 读不到任务清单时**一个都不删** —— 宁可留着占地方，也不能把素材当孤儿清掉

### 顺带

路径从此**只此一处算**（`TaskMedia`）。此前 12 个地方各写各的
`material_cache/<id>.mp4`，改一个漏一个 —— 今天已经因为「同一个东西两处算」
出过静音和配音过期两个 bug。

## 0.1.60

### Agent 补课：最近四版新增的能力，它一直不知道

你问「每改一个功能，Agent 的能力有没有同步」——查下来**没有**。
0.1.56 起新增的东西，Agent 侧基本全部掉队，其中一条还是会静默出错的 bug。

**最严重的一条（已修）**：界面说「这几句配音过期了，要重配」，CLI 却报
`state: fresh`。因为 CLI 拿行上的值判断，不知道本片基调换过。Agent 读到
fresh 就不重配，**直接导出一条前后两个人说话的片子** —— 不报错、不拦截，
人拿到成片才发现。

**另一条更大的**：Agent 装完技能手册，里面 `ishkafel script` 一个字都没有。
脚本成片那条线（14 个子命令）的说明书躺在 `docs/` 里**没有任何交付通道**，
等于整条线对 Agent 不存在。现在和主手册一起交付。

补齐的能力：

| 它现在能 | 命令 |
|---|---|
| 读到本片音色/语速基调 | `script show` 的 `defaultVoiceId` / `defaultSpeechRate` |
| 读到三条轨的音量与静音 | `script show` 的 `sound` |
| 读到每行实际会用谁的声音 | 行里的 `voiceId`（有效值）+ `voiceFollowsDoc` |
| 定本片音色/语速 | `script apply baseline` |
| 改某几行的音色/语速 | `script apply line-voice` |
| 调三条轨的音量/静音/闪避 | `script apply mix` |
| 把方案写成剪映草稿 | `script jianying` |

手册里也写清了三条轨分别是什么（原声=素材自带的声音、口播=念台词的、
配乐），以及**收工前要检查有没有 stale 配音** —— 这一条不拦导出，所以更危险。

### 修：一条会被你「用软件」弄红的测试

剪映那条端到端测试直接读你机器上**活的**任务文件。你在界面上动一下那条
片子，它就红一次（这次是字幕从 62 屏变成 60 屏）。而且它在别人机器上会
跳过，等于只在一台机器上有效。

现在数据固定进仓库，素材用占位文件顶上：谁改自己的片子都不影响，同事
机器上也真的会跑。

## 0.1.59

### 新增：把方案直接交给剪映

编导台顶栏多了一个「**剪映**」按钮（在「导出成片」右边）。点它会把当前
这条片子写成一份**剪映草稿**：画面段、字幕屏、配音、配乐、素材库全部就位，
打开剪映就能接着精修。

- 素材会先补齐并落到本地，草稿里引用的都是本地文件
- **不会替你拉起剪映**：剪映没有给外部程序「打开指定草稿」的通道
  （各种办法都实测过了）。它自己扫草稿目录——启动时必扫（8 秒内认领），
  运行中每隔几分钟扫一次。所以生成完会把**草稿名**告诉你，去剪映草稿
  列表里找那个名字打开即可

> **这个按钮的点击流程还没在真机上走通过一次**，请你点一下试试。
> 它有 25 个单元测试，也拿 2 号任务的真实数据跑过端到端（143.76 秒 /
> 55 个画面段 / 62 屏字幕 / 27 段配音 / 4 段配乐 / 83 个素材），
> 生成的草稿在剪映里打开播放确认过——唯独「在界面上点这个按钮」这一步
> 没能自动验证（副屏的点击坐标映射不准）。

### 这一版还包含 0.1.48 起积压的全部改动

你手上一直跑的是 0.1.56（而且是从另一个工作区构建的），所以下面这些
你可能都还没见过：

- **本片基调**（0.1.57）：顶栏「本片 · 云希 · 1.25x」，铺一版前先定音色
- **多选批量**（0.1.58）：Shift / ⌘ 点选、⌘A 全选，底部操作条改音色/语速/删除
- **播放到哪展开哪**（0.1.58）：播到哪一镜，那一镜的操作栏就摊在手边
- **三轨混音台**（0.1.56）：原声 / 口播 / 配乐，各有总音量与静音
- **任务编号徽章**（0.1.55）：进任务也看得见 #N，点一下复制
- **审片台的 Agent 操作**（0.1.54）：人看着卡片墙指挥 Agent 剔除

## 0.1.58

### 改 5 句不用点 5 次了

选行用的是你本来就会的手势，**没有「批量模式」这种东西** —— 就像 Finder，
选一个文件和选十个文件，操作是同一套：

- **Shift + 点击** 选到那一行为止的一段
- **⌘ + 点击** 加选 / 取消某一行
- **⌘A** 全选，**Esc** 取消选择

选中两行以上，底部浮出一条操作条，只有三件事：**音色 / 语速 / 删除**。
它们的共同点是一行行点特别痛。字幕样式已经有「整片」了；配乐是段的概念、
不是行；时长每行都不同，批量设没意义 —— 所以都不放进去。

改完音色或语速，会问一句「这几句要重新配音吗」，重配**带进度、随时能停**。

删除会说清删掉的是什么：「其中 3 句已经配好音，删掉这些配音也一并没了」，
而不是只说「删除 8 行」。删错了 ⌘Z 撤销。

### 播放到哪，那一镜的操作栏就摊开在手边

以前播放只是把那一行选中，你还得自己点开才能改。现在播到第几行第几镜，
那一镜的取段、速度、原声就直接展开在眼前。

**你一动手，自动展开立刻让开** —— 不然你正改着第 3 行，播放头走到第 5 行
就把你手上那一栏收走了。按暂停就恢复跟随。

### 「配音」轨改叫「口播」

三条轨并排时，「原声」和「配音」太容易混。软件内部一直管它叫口播轨，
现在界面也统一了：**原声**（素材自带的声音）/ **口播**（念台词的）/ **配乐**。

### 修：换过音色的旧配音文件，一直没人清

每换一次音色就多一批死 mp3 躺在盘上。孤儿清扫只管「归属不到现存任务」的
整个目录，任务还活着时它一个都不碰 —— 所以这批文件谁都不管，批量换 28 句
就是 28 个死文件。

现在每次进任务顺手收一次。**只在进门那一刻收**：编辑当中删会让 ⌘Z 撤销
撤成死链（真机上出现过整行无声）。

## 0.1.57

### 这个片子听起来是谁在说话，现在有个地方定了

以前音色只能一行一行选。铺一版的时候软件拿目录里第一个音色去配 27 句 ——
不对就白烧一轮语音合成，然后你还得点 27 次改回来。

现在编导台顶栏常驻一枚「**本片 · 云希 · 1.25x**」：

- **铺一版之前如果还没定过音色，先卡住问你一次** —— 这就是「不浪费一次生成」
- 新写的行、新生成的配音，都跟着本片基调走
- 某一句要不一样，仍然在那一行单独改；改过的行会标出来
- 从顶栏改基调，或在某一行勾「应用到整片」，都是**定基调**而不是逐行写死：
  之后新加的行也跟着走，不会留在旧音色上

### 改了音色，已经配好的那几句会问你一句

这是之前会静默出错的地方：改完基调，前 10 句还是旧音色、后 18 句是新的，
**混出一条前后两个人说话的片子**，而且最容易一路漏到成片。

现在改基调的那一刻会问：「已经生成的 10 句还是旧音色，要不要一起换？」
选重新生成就批量跑，**带进度、随时能停**，停下时已经生成好的保留。

### 语速也一样有本片基调

和音色同一套规则。顺带修了一个隐藏的语义问题：以前语速「没设过」和
「设成原速」是同一个值，所以单独把某一句调回原速做不到 —— 现在分开了。

## 0.1.56

### 主预览下面变成真正的三轨混音台

你在行内把原声调到 0，回到主预览下面拉那根「原声」滑杆——**一点反应
都没有**。查下来不是冲突，是那根滑杆名不副实。

它调的从来不是「原声轨总音量」，而是**「配音行没单独设时，原声压到
多少」**（默认 0，防止原声和口播叠成两份声音）。画面行没有口播、压根
不参与这条规则，所以拉它对画面行永远无效。它只是叫着「原声」、
摆的位置像总音量。

现在按你说的那张混音台重做：

```
原声  ●━━━━━━━━━○━━  75%   🔇
配音  ●━━━━━━━━━━━━  100%  🔇
配乐  ●━━━━━○━━━━━━  45%   🔇

☑ 有口播时把原声压到 0%   ●━○━━━
```

- **三条轨各一根总音量 + 各一个静音钮**，对所有行生效，拉哪根都有反应
- 静音是「临时闭嘴」：滑杆位置不动，再点一下回到原来的地方
- 逐镜原声、逐段配乐是**相对值**，乘在总音量上——所以单独调过的镜头，
  拉总音量也跟着变
- 原来那个「口播时压低原声」独立成一行，回归它本来的名字，压到多少
  可调（不再只能 0 或全开）

预览、行内播放、成片导出三条路读的是同一份混音设置。

**老片子的声音一个字节不变**：三条总音量默认 100%、闪避默认开且压到 0%，
和升级前完全等价；老档里的 `sourceVolume` 原样读成闪避电平。

## 0.1.55

### 任务编号：进到任务里也看得见，点一下就复制

你跟我（或跟 Agent）说事情报的是「#12 的第 3 句」，可进了编导台/工作台/
审片台就找不到自己在第几号任务上了——工作台和审片台根本没显示，编导台
虽然有，但用的是最淡的一档灰，等于没有。

现在三个模块的顶栏最左边统一有一枚编号徽章，**点一下就复制**，直接粘给
对方。还没补号的任务显示 id 前段，复制的是完整 id。

### 修：画面行的原声调不响

真机 bug，根因是**界面在撒谎**。画面行（没有台词的纯画面行）：

- 滑杆显示的默认值取的是「全片原声基调」（默认 0，写着「静音」）
- 实际播放用的却是满音量

同一个东西两套默认。全片基调是 0 的时候，人看到滑杆写着「静音」，
就去拖它想修好——**一拖就把一个显式的 0.0 写死在那一镜上**，从此那一行
真的哑了，而且之后把全片原声调到 100% 也救不回来（逐镜的设定压着全片）。

这一版把「这一镜该出多大声」收敛成唯一一处：画面行默认满音量、配音行
默认跟随全片基调，滑杆、行内预览、整片预览、成片导出读的都是它。同时
把静音标成警示色，「恢复默认」做成看得见的按钮——被写死的那几镜，
一眼能找到、一点就救回来。

**已经被写死的镜头不会自动改**（也许你真想让它静音）：在那一镜的原声
滑杆右边点「恢复默认」即可。

## 0.1.54

### 审片台、导入、导出也能让 Agent 来做了

上一版说这三个模块「性质不同，优先级低」——那个判断是错的。判断标准不该是
「这个模块是干什么的」，而是**人会不会在这里指挥 Agent 干活**。

- **审片台**：人看着卡片墙说「第 3、7、12 条删掉」，Agent 去删。
  `review list` 看编号、`review drop/keep` 动手，编号写错整批不落盘
- **导出**：分钟级的活儿，进度实时显示在界面横幅上，人不用盯着终端
- **导入**：软件自己弹出来说「正在导入 xxx」，建好之后把人带到新任务上

### 人正开着界面时，Agent 不再是「写不进去」

以前人一打开审片台，任务就锁了，Agent 的写入直接被拒——可人正是开着它
在指挥 Agent。这一版改成**委派**：界面开着就把活儿交给界面去做。

这不只是绕开锁。审片台上剔掉的卡本来就只是**界面里的临时状态**，人按
「确认」才落盘——Agent 自己写盘会让人还没确认盘上就变了。委派之后，
人看到的和最终结果永远是同一份东西。Agent 那头也会如实回报「已经标好了，
按确认才生效」，不会报成「已删除」。

### Agent 占着任务时，界面不再是一张空白拦截页

可视模式下人正是为了看它干活才打开那一页的，拦掉等于把要看的东西挡在
门外。现在改成**只读跟随**：候选照常显示，它动哪张卡就滚到哪张卡，
横幅写着它这一步在做什么，它一收工页面自动恢复可操作。

## 0.1.53

### 可视模式不再只有编导台

上一版做了两种模式，但只有编导台会跟着 Agent 走。这一版把那套地基做成
**全软件共用**的：

- Agent 说「去编导台第 10 句」「去工作台第 3 个单元」，界面负责怎么去——
  没开就拉起来、人停在别的页面就先退回来、再进目标模块
- **工作台也接上了**：它是时间线式的，所以「看第 3 个单元」就是把播放头挪
  到那儿；顶部横幅除了说谁占着，现在还会写出**它这一步在动什么**
- 同一个模块里连着做几步不会重复弹窗，只有换模块才会把人带过去

各模块只负责「我怎么把那个位置摆到眼前」——编导台按行滚、工作台挪播放头——
共用同一份状态与同一套回执，不各造一套。

## 0.1.52

### Agent 干活的两种模式

以前只有一种：Agent 在后台改数据，界面上顶多显示一句「它在忙」。现在分成两种，
**由你对 Agent 说话时决定**：

**静默**——人不在场。它跑完，你回来看结果或直接看成片。不弹窗、不打扰。

**可视**——你说「我要看着你做」。这时候：

- **软件会自己弹出来**，并落到它正在做的那个任务上
- 它动哪一行，界面就滚到哪一行；它开哪个面板，界面就打开哪个——
  和你自己点开时是同一个界面
- **每一步都等界面真的展示完才继续**（滚动停下、面板展开）。不是定时等几百
  毫秒——机器快的时候不白等，慢的时候也不会一闪而过没看清
- 这期间你点不动软件（两边同时写会互相覆盖），但**你随时能在 Agent 那头喊停**：
  一按停止它立刻放手，界面马上能动，你可以自己改，改完再让它接着看

就像站在实习生旁边看他干活——随时能打断、帮一把、纠正一把。

## 0.1.51

### 脚本任务在列表里有封面了

排好 27 句、配好镜头的片子，在任务列表上和一个刚建的空任务长得一模一样——
都是黑块，看不出哪条做过。

现在脚本任务的封面取**成片的第一帧**（第一行第一镜的画面，从这一镜实际用到
的那一刻取，不是素材开头）。挑完镜头就有，换了第一镜会跟着换；还没配镜头的
空任务仍然是黑的——那是对的。

## 0.1.50

### Agent 现在能独立做一条片子，而且你看得见它在做什么

编导台的全部能力开放成了命令行，任何能跑 bash 的 Agent（Claude Code、
Codex、别的）都能驱动：

```
script new / extract / voice / export     建任务、识别台词、配音、导出
script show / shots / subtitles           看懂任务、拿挑镜头与断句的依据
script apply shots / subtitles / alloc / bgm / lines / shot-edit / screen-text
```

**它干活时，界面跟着它走**：顶部横幅写出它正在做什么，右栏滚到它正在动的
那一行，展开它正在调的那一镜——和你自己点开时是同一个界面。这期间你点不动
（两边同时写会把彼此的活覆盖掉），右上角有「我来接手」随时抢回来。它要是
崩了，一分钟后界面自动解除，不会被永久锁住。

### 哪些交给它、哪些不交，是按「能不能验证」划的

能验证输出的才交出去：挑镜头（素材 id 必须在候选里）、字幕断句（切点是词
序号，递增、在范围内、每屏字数够不够一查即知）、分时长（总和必须等于配音
时长）、配乐分段（区间要连续铺满全片）。

识别台词和配音**不交**——时间戳偏两百毫秒就足以毁掉整条片子，而这个错不会
报错，一路静默到看成片才发现。

**字幕断句是它比软件强的地方**：软件的自动分屏是「标点优先、其次停顿、
最后按字数硬切」的笨办法，本质在猜；而断句是语言判断。

### 提交不合格会整批拒绝，并一次点全所有问题

```
这一批没有落盘（整批拒绝，下面是全部问题）：
· 第 2 行的切点 18 超出范围（这句共 9 个字）
· 第 2 行第 2 屏只有 1 个字——闪一下就过去，看的人只会觉得晃眼
```

改一条提交一次是在浪费双方时间。

## 0.1.49

### 想把中间几句换首配乐，不用再切两刀了

配乐是把整片切成连续几段、每段一首曲子。以前想把第 8~10 句换成另一首，
得先在第 8 句切一刀、再在第 11 句切一刀、然后点中间那段选曲——三步，
而且两刀切在哪很容易点错。

现在点开任意一段的选曲面板，顶部就能直接改「这段配乐管第几句到第几句」，
改完相邻的段自动让位（被盖住一半的缩短，被完全盖住的消失），整条轨仍然
连续铺满全片。

## 0.1.48

### 三路声音彻底分家，画面行的原声终于能逐镜调

接着上一版：上一版把画面轨的音频关掉了，这一版把剩下两处同样的毛病也治了。

**问题是同一个**：一条声音轨上不能混不同格式的音频——播放器一遇到格式变化
就要停下来重搭链路，画面卡住、声音被反复拽回同一处。除了画面轨，口播轨上
也有两处：画面行用的是素材（mp4）、配音行用的是合成的 mp3；配音比画面短时
还会插一段格式不一样的静音。

现在的分工是：

- **口播轨只放配音**，画面行在那一段垫同规格的静音
- **素材原声走独立的一条轨**，配音行和画面行都走它——所以「这一镜的原声调到
  35%」才真的能生效，而且**拖着滑杆就听得到**，不用松手也不用等
- **配乐轨照旧**

进口播轨的每一段都预先转成同一规格并补足到坑位长度（短了垫静音、长了截断），
从此既不留空档、也没有格式接缝。

### 镜头卡上播的，和成片听到的一致

以前在镜头卡上点播放，素材原声是**满音量**播的——你把原声调到 35%，主预览变了、
卡上没变。现在卡上播的音量与成片完全一致，调音量时正在播的那一镜也当场跟着变。

### 编导台选配乐改成单选

选配乐的面板是和成片翻新那条线共用的：那边一个任务要导出好几条片子、每条配
不同的曲子，所以能选多首。**编导台是一条片子**，一段配乐就一首——代码里一直
只用第一首，多选出来的其余几首根本不会生效。现在编导台这边只能选一首。

## 0.1.47

### 修掉预览里口播「卡住反复念同一个词」

反馈的现象：播到第 9 秒，「还送」连着重复十来遍，然后才接着往下播；有时候
表现得轻一些，就是声音忽大忽小。**同一条片子导出成片完全正常。**

抓到的现场数据是这样的：

```
主时钟 9955ms  ← 连续 22 次纠偏里，它一动不动
口播轨 10383ms ← 也不动，怎么拽都拽不回去
```

**画面轨卡死了。** 它把几十条素材拼成一条流播放，而这些素材有的带音轨、
有的不带；播到「不带 → 带」的接缝时，播放器要重新搭一遍音频链路，画面就在
那里停住好几秒。画面是整条片子的时钟，它一停，口播就被反复拽回同一处——
听起来就是一个词反复念。这条片子里有 16 处这样的接缝，第一处正好在第 2 句。

这一版把声音彻底拆开：**画面轨不再解码任何音频**，接缝也就不存在了；
素材原声挪到独立的一条声音轨上，跟着镜头走。现在口播、原声、配乐三路
各走各的，互不影响。

修完实测：同一段播放，纠偏从 22 次降到 1 次，偏差稳定在 100 毫秒以内。

## 0.1.46

### 修掉预览里口播「忽大忽小、卡住重复」

反馈的两个现象其实是同一个毛病：预览播着播着，口播声音忽大忽小；有时候卡在
某几个字上反复念（「还送、还送、还送……」）。**同一条片子导出成片完全正常。**

根子在**变速镜头的预览切片短了几帧**。给变速镜头做预览时，会先渲一段
「时长正好等于坑位」的对齐切片；但视频时长只能是整帧的倍数、坑位是任意毫秒数，
ffmpeg 截出来就比要求的短了 15~49 毫秒。画面轨是整条片子的主时钟，它播到这些
镜头就提前收，位置一路偏快；口播轨按主时钟对齐，被反复往前拽——偏差累积到
一定程度就会来回越过纠偏线，于是声音一会儿被拽一下。

导出侧一直是**克隆最后一帧补足**的（所以成片没事），预览这边漏了这一步。
这一版补上，并且渲完当场量一次：切片比坑位短就记进日志点名。

实测这条片子里 7 个变速镜头有 5 个原来都偏短，现在全部够长。

### 改设置不再连累正在播的预览

以前改任何东西——哪怕只是调个语速、改句字幕、换个标签——都会在 0.6 秒后
把整条预览轨重铺一遍。正在播的时候重铺，画面和声音都会被扰一下。现在只有
真正影响画面/声音编排的改动才重铺。

- 重铺期间播放器下方会显示「正在重铺预览…」，不用再猜是不是没点中
- 改语速会当场告诉你「已设为 1.25x——点这一行的『重新生成』才会按新语速配音」，
  因为语速只在下次生成配音时生效

## 0.1.45

### 「生成草片」改成「自动铺一版」，配镜照着参考片来

**名字变了**：「草片」是行话，也没说清点了会发生什么。「铺」对应的是
把整片的空位填满、再逐句替换；「一版」是说它可以改，不是终稿。

**配镜的依据换了，这是这一版的重点。** 以前是「有标签就按标签搜，
否则拿台词去搜画面描述」——后者是个错配：台词说的是「如果你觉得有点贵」，
而素材库里存的画面描述是「手持喷雾瓶在厨房台面」，一句话和一幅画根本不在
一个维度上，搜出来的东西和这句话没关系（而且只取第一条）。

现在走的是这个产品真正的主线——**复刻参考片**：先看懂参考片这一镜长什么样
（多帧识图，一次拿到画面描述和画面标签），再拿这个描述去妙啊找像的画面。
和你在「找镜头」面板里手动做的是同一条路，只是批量跑一遍。

**手写脚本不再自动铺镜头**：没有参考片就没有依据，与其瞎捞一堆不相干的
画面让你逐个删，不如空着——空着一眼就看得出「这里还没做」。这时候会明确
告诉你：去右栏「找镜头」挑，或者先上传一段参考视频。

## 0.1.44

### 找镜头面板：参考镜头也能就地播，卡片放大

- 上面那排**参考视觉镜头**每一镜都能点开看了：点缩略图上的播放键，
  画面就在那个位置上播这一镜（播的是原片的对应区间），再点停
- 参考镜卡片放大（宽 168→232、整排高 96→132、缩略图 48→78）：
  它是这个面板的主线索——默认就拿它去找像的画面，看得清才知道要复刻什么
- 点卡片其余地方仍然是「用这一镜的画面去找」，播放键没有抢掉主操作；
  参考镜和候选卡共用一个播放器，同时只有一个在响

## 0.1.43

### 修掉预览里「台词最后几个字一直重复」

反馈的现象很怪：预览播到后面，台词的最后几个字一直重复，但**同一条片子
导出成片却完全正常**——画面对、声音也对。

原因在预览和导出拼声音的方式不一样：

- 导出是逐行渲染，每一行的声音都补齐到画面那么长，严丝合缝
- 预览是把几十段配音**首尾相接**成一条流播。这种拼法没有「空档」的概念——
  某一行的配音比画面短（比如把镜头调长了、或者用过「放慢充满」），
  那个空档会被直接压掉，从这一行起后面每一句的声音都提前一截。
  累到片尾，声音已经播完而画面还在走，播放器就被反复拽回末尾——
  听起来就是最后几个字一直重复

这一版把声音轨的空档用静音垫平，声音和画面严格一样长。另外加了一道自检：
以后任何一条轨上留了空档都会记进日志，不用再靠现象倒推。

## 0.1.42

### 找镜头面板：看得更多、能就地看片、搜不到时有兜底

- **弹窗按屏幕比例放大**：以前是写死的 920×660，一屏只看得到六七条，
  197 条候选要翻九页。现在按屏幕的 88% 铺开，一屏能看到十几条，
  每页也从 20 条提到 40 条，翻页次数减一半
- **候选卡能就地播**：卡片中间有播放键，点一下就在这张卡上播，再点停。
  不用再为了看一眼素材开关一次播放窗
- **一键清空筛选**：标签勾窄了搜不到东西时，「清空筛选」把约束全松开重来
- **多了「按名称」这条兜底路子**：主路径还是拿参考镜头的画面描述、标签
  去找像的画面；实在筛不到时，直接输入妙啊里的文件名把它捞出来。
  切到按名称会自动摘掉标签约束（都按名字找了，还挂着标签只会继续搜不到），
  切回别的维度时标签原样还回来

## 0.1.41

### 素材原声可以留下来了：原声 + 口播 + 配乐三者共存

以前分镜素材自带的声音是被直接丢掉的，配上背景音乐之后就更听不到了。
可素材里常有音效——喷雾声、开门声、翻冰箱的窸窣声，全丢掉片子会发干。

- **预览下方多了一根「原声」滑杆**：管整片的素材原声出多大，0 就是静音。
  拖动即时生效，口播、配乐、原声一起响
- **每一镜可以单独开小灶**：镜头详情里也有一根「原声」滑杆——这一镜有音效
  就推上去，那一镜背景嘈杂就压到静音，设过的显示「跟随整片」可以一键还原
- **默认仍是静音**：升级不会改变你已经做好的任何一条片子的声音，想要就推上去

两路声音各保各的音量，不会出现「一开原声，口播就变闷」。

## 0.1.40

### 画面铺不满的镜头，现在会被拦下来

真机撞到的一个坑，两副面孔，很难联想到同一个原因：

- 预览播到第 15 句，声音变成「现在我们家每个月每个月每个月……」一直重复到这句结束
- 同一句导出成片，声音正常，画面却在「每个月」那里**定格**，直到下一个镜头才动

根子是**这条素材只有 1.2 秒，却被分了 6 秒的坑位**。素材时长原本靠对着网络地址
去问，网络一抖就问不到；问不到就被当成「有多长都行」，于是想分多少分多少。
成片那边只能拿最后一帧撑满剩下的 4.8 秒（定格），预览那边画面提前放完、
配音被反复拽回同一处（听起来就是台词重复）。

这一版：

- **素材下载到本地就量一次真实时长**——文件都在本地了，没必要继续猜
- **铺不满就是阻断**：这一行不进预览，中栏明确写出「第 N 镜的画面只有 1.2 秒，
  铺不满 6.0 秒（差 4.8 秒）——换条长一点的素材、放慢这一镜，或把它截短」
- **导出同样拦下**，点名到行和镜。宁可不出片，也不出一个定格几秒的片子

## 0.1.39

### 配音「念岔了」不再蒙混过关

反馈的现象：一句话前半句正常，后半句两个词一直重复到结尾。这是大模型
语音合成的老毛病（解码陷进循环），也会表现成念到一半就断。以前这种音频
会照样存下来、照样进成片，只有靠人听才能发现。

- **生成时就自查**：合成完本来就会把音频转写一遍（为了拿到逐字时间），
  现在顺手把「念出来的」和「写的」比一比。念重复、没念完、语速离谱的，
  **自动重来一次**，第二次好了就用第二次，坏的那份立刻删掉
- **两次都念岔就明说**：给出是哪一句、岔在哪儿，并建议把台词拆短再生成。
  绝不把卡住的声音放进成片
- **老配音也体检**：已经躺在方案里的配音同样按这套判据检查，念岔的行上
  直接标红「这句念岔了」，鼠标移上去说清原因和怎么办——不用等你听到
- 判据刻意放宽：识别把「10瓶」听成「十瓶」、「厨垫」听成「橱垫」这类
  同音差异一律放过，只抓一眼就不对的（字数暴涨、明显截断、语速离谱）

## 0.1.38

### 导出不再因为「素材还在下」把你赶回去

以前点导出，只要有一首配乐或一条素材还没下完，就弹一句「还没下载到本地」
加一个「知道了」——你只能自己瞎等，再点一次试试。现在：

- **点了导出就是要导**：还在下的，进度条上写「正在准备素材 3/12」，
  下完自动接着导，不打断
- **只有真下不下来才停**，而且说清是哪一段、为什么（登录过期 / 网络不通 /
  素材库里已经没有这条），并且给能点的按钮：**重试下载**；
  卡住的如果全是配乐，还可以**去掉这几段配乐直接出片**
- 素材本来就是「挑中即下载到本地」的（别人在素材库那边删掉也不影响你出片），
  这次补的是**等待与失败的交代**

### 配乐下载状态看得见了

分镜卡一直有下载状态（转圈 / 失败标红 / 点一下重试），配乐色带以前什么都没有
——曲子下砸了全程无感，直到点导出才知道。现在配乐色带也有：
正在下就淡出并转圈，下砸了整段标红打叹号，**点一下就重下**。

## 0.1.37

### 字幕分屏：长镜头下的长台词，一屏一屏地出

同事反馈的场景：一个镜头很长（十秒盖过五六句话），字幕却整段堆在画面上。
这一版把「屏」变成字幕的基本单位——**切点跟着语言走，跟镜头没关系**。

- **自动分屏**：一句话按句读与字数上限切成若干屏，依次出现。每屏能装几个字
  由**当前字号**决定：字调大，屏自动切得更碎，字不会出画
- **镜头时长随便改，字幕自愈**：屏记的是「从哪个字另起一屏」，不是时间点。
  把一镜从 4 秒缩到 1.6 秒，字幕不会重复也不会丢
- **手动优先**：镜头详情里一屏一行，左边是它在成片里的入点，中间直接改字
  （只改这一屏，不用整段重打），行尾三个动作——拆成两屏、并回上一屏、
  这屏不出字。改过的行标「已手改」，一键「恢复自动」
- **打轴**：原位播这一镜时点「✂ 在这里换屏」，听到哪儿切到哪儿；
  切不动会说清原因（这里已经是屏首 / 这句配音没有逐字时间）
- **四处显示同一份**：镜头卡缩略（首屏 + 「+N屏」）、卡上播放叠的字
  （跟着播放位置换屏）、中间预览、导出成片——一字不差

### 修掉三个会让字幕出错的老问题

- **一个字一屏**：逐字停顿一样大时，自动分屏会把第一个字单独切出去，
  一句话炸成一列。现在屏尽量装满
- **价格被写错**：语音识别会把「69.9一」并成一个词，照词拼接就出「69.91瓶」。
  现在字幕一律**按脚本原文**出字
- **老配音整段堆字**：早期生成的配音没有逐字时间，以前只能整句一屏。
  现在照样分屏（按字数摊时间），界面上写明「按字数均分」，
  重新生成配音后就能手工分屏与打轴

## 0.1.36

### 编导台大更新：取段所见即所得、参考按镜头找、配乐色带、导出可选规格

**镜头取段**
- 取段条上的数字全部是**成片时间**：整条 = 这条素材按当前倍速能出多长，
  蓝框 = 这一镜在成片里的时长。「1.5 秒就是 1.5 秒」，与倍速无关
- 条上铺的是**素材真实画面**（帧格锁素材比例，窗口拉多宽都不拉伸），
  窗口外压暗——拖到哪看到哪
- 拖完松手自动播一遍**调整后**的这一镜（画面按倍速播 + 这一段的配音），
  播一次就停；连着拖只在最后一次之后播

**原位播放（不再弹窗）**
- 参考卡、镜头卡点一下就在卡上播：镜头卡播的是「成片里这一镜的样子」
  ——画面 + 这一句配音里属于它的那一段，卡底还叠着这一镜的字幕
- 三个声源（分镜卡 / 主预览 / 配音试听）互斥：谁开始播，其它自动停

**字幕**
- 预览下方常驻**字幕工具条**：位置、字号都是滑杆（连续、实时），
  六色、遮罩三档，右端「应用到整片」；调的时候预览实时变
- **每张镜头卡上直接显示这镜的字幕**（位置/颜色/遮罩按真实样式缩放），
  不打开面板就能一眼扫完整句每镜显示什么字
- 手改规则：手写或点「同上一镜」就脱离自动匹配（标「已手改」，
  可一键恢复自动）；没手改的永远跟着时长实时重算

**添加分镜（原「增删」）**
- 弹窗里的参考 = 这句在原片里的**各个视觉镜头**（进去就切、loading 等它，
  不再是关掉重开才对）
- **点哪个参考镜头，就用哪个镜头的画面条件搜**（它的画面描述 + 标签，
  按需 AI 读一次画面并缓存）；一个都不选就用这句的标签 + 脚本台词
- 参考里的台词只展示、不参与检索；每镜可「直接用原片这段」

**参考素材**
- 每句的参考槽位可以传**视频或图片**（只能一个，替换先确认）；
  手动传的视频不超过 15 秒，传完自动识别台词（仅展示）+ 切视觉镜头

**配乐（重做）**
- 行块左缘一条**配乐色带**：同段同色、相邻段异色。鼠标移上去，
  在要换歌的那一句点「✂ 从这句换一首」——30 句配 3 首只要切两刀
- 段首点色带：换曲 / 音量 / 设为这段不要配乐；顶栏「配乐」= 给整片配一首
- 主预览按段播配乐、按段应用音量

**导出**
- 导出前先选规格：分辨率 / 帧率 / 码率（可自定义）/ 编码 / 格式，
  「预计大小」实时算；记住上次的选择

**顶栏瘦身**
- 只留「配乐 · 生成草片 · 导出成片」。字幕样式归工具条；
  「从视频提取脚本」是空脚本时的一次性起步动作，回到起步引导里
  （它会覆盖整份脚本，不该常驻）

**修复**
- 第 2 句台词整段无声：撤销把数据回滚到了已被删除的旧配音文件——
  重新配音后不再立即删旧文件，配音文件丢失的行也会点名而不是装哑巴
- 镜头卡黑图：缩略图改用本地素材抽帧（原来的签名地址会过期）
- 右栏点击间歇性整体失效（Flutter 线程合并实验特性，已关闭）
- 字幕里的 69.9 被显示成 699（剥标点误伤数字小数点）
- 拖到底线时「调不动了」提示连环弹

---

## 0.1.35

### 编导台：字幕模型定稿——字幕写在镜头上、样式按句统一

- **字幕写在镜头上（讨论定稿）**：每个镜头带自己的字幕——镜头详情
  里的「字幕」框**自动预填**这镜时段说出口的字（听一遍对不对即可），
  改了以你写的为准，清空恢复自动；「同上一镜」一键让几个镜头共用
  一句。相邻镜头同句时字幕连成一条不闪断。预览与成片同一份派生
- **样式粒度 = 句**：位置/字号/颜色/遮罩每句统一一套——预览里
  **点字幕改的就是正在播的这一句**（所见即所改），顶栏字幕按钮改
  整片基调；句级面板新增「应用到整片」一键提升为全局默认
- 「分小行」那套（切点选择器）退役——镜头级字幕覆盖其全部场景
- 修：拖取段条到底线时提示连环弹（一次拖拽攒一队列 SnackBar）——
  节流为 3 秒最多一条

---

## 0.1.34

### 编导台：镜头取段直接拖、台词可以分小行指定给镜头

- **取段条（新）**：镜头详情里的「起点滑杆 + 时长步进」换成剪辑软件
  式的取段条——底条是素材全长，蓝色窗口是实际用的那一段：**拖窗口
  整体平移**换起点（在素材上滑动裁切）、**拖右缘把手**改时长
  （多退少补邻镜自动配合）。4 秒素材要哪 2 秒，拖一下就框好
- **台词分小行（新）**：一句台词（台词语义单元）里可以把文字分段
  指定给镜头——「家人们」给前两镜、后半句给第三镜。点开镜头详情
  「✂ 从这一镜分小行」，切点选择器会按配音说到哪个字自动建议，
  点个字确认即可。分组后块里每小行显示「这段台词 + 它的镜头」，
  段文字旁悬停「并回上行」随时合并
- 字幕跟着分组走：预览和成片里这段字横跨组内所有镜头连续显示
  （手动指定优先于自动按词切分）
- **取段条铺的是素材真实画面**：胶片形态——窗口外压暗、窗口内就是
  选中的那段画面，拖到哪看到哪；配「起点~终点 / 素材可用 / 选用」
  三个数字口径（对齐参考图）。帧按素材缓存，算过一次不再抽
- **修灾难级 bug**：右栏行带板的鼠标点击会**间歇性整体失效**（同一
  构建时死时活）——根因是 Flutter「UI 与平台线程合并」实验特性，
  已在 Info.plist 关闭，多轮重启验证不再复现

---

## 0.1.33

### 编导台：分子/原子各归其位——外面是分子列，原子在搜索里当条件

- **行块收敛回分子粒度**：参考不再在块里铺格子——一张整段参考卡
  （「参考 8.0s · 4 镜」），点击播放对照。上一版挤成一团的胶片条废弃
- **参考原子进找镜头面板（新）**：给这句话找镜头时，面板顶部横排
  这一句的参考分镜（原子）——每个原子带首帧和**它那个时段说的话**
  （新提取的脚本自动带词级时间戳）。**点选哪个原子，检索词就切成
  哪个原子的台词**，一个原子一个原子地找替代品；再点一下回到整句
- 原子上还有「直接用原片这段」：本地引用原片区间作镜头，和搜索
  结果一起进已选序列
- **块内左右结构**：左边参考卡（与镜头卡同规格竖屏摆放、灰调+
  「参考」角标、点击播放整段）、右边我的镜头行，中间一条呼吸缝——
  原文｜译文左右对照
- 修：参考缩略图抽帧失败会无限重试起 ffmpeg（每帧一次）——失败记账
  后不再重试

---

## 0.1.32

### 编导台：字幕按镜头切分、字幕直接上手调、参考变胶片条

- **字幕跟着镜头走（新）**：一句台词里有几个镜头，字幕就按说话的
  节奏分到镜头上——「家人们」只在第一镜出现，后半句归后面的镜头。
  依据是配音的**逐字时间戳**（新配音自动带上；老配音重新生成即可），
  预览和成片用同一套切分规则。没有时间戳的老配音仍整句显示
- **字幕就是操作对象（新）**：预览里的字幕**点一下**直接打开样式
  编辑；**上下拖**直接调位置（拖的是行级就改行级，否则改全局），
  不用再去顶栏找按钮
- **参考重做成胶片条**：这一句在原片里的完整区间是一个整体（竖排
  「参考」标签），内部按视觉切点分格——一眼读出一句话在原片换了
  几个镜头；格宽随镜头时长成比例，悬停出现播放与「用它」。
  参考（原文）在上、我的镜头（译文）在下，对照着配

---

## 0.1.31

### 编导台：预览里能看到字幕了（所见即所得）

- **实时字幕层（新）**：预览播放到哪一句，字幕就显示那一句——位置、
  字号、六色、形态（描边/黑条/**毛玻璃**）与成片同一套参数，改样式
  **即改即见**，不用导出成片才知道字幕长什么样。行级字幕覆盖同样生效
- 中栏舞台加深一级（比工作栏更沉），预览成为视觉中心
- 行块与镜头卡有了悬停反馈

---

## 0.1.30

### 编导台：换音色可一键应用到整片

- 音色选择器多了「**同时应用到整片**」勾选：整片统一声音是常态，
  不用一行行换 27 次。换完旧配音自动标黄，点「生成草片」一键全部
  重新配音（顶栏按钮也会自己变回主角提醒你）

---

## 0.1.29

### 编导台：草片完成有仪式感、主按钮分清主次

- **草片做完有个「好了」的瞬间**：进度收束成一个弹入的绿色对勾
  （「草片好了，这就播给你看」），一拍之后直接开播——不再是列表
  默默刷新
- **顶栏主次动态**：还有句子没配好时「生成草片」是实心蓝主按钮，
  全就绪后让位给「导出成片」——一屏永远只有一个主角

---

## 0.1.28

### 编导台：快捷键与撤销

- **⌘Z 撤销 / ⇧⌘Z 重做**：编导台里的每一步改动（删镜头、改时长、
  换标签、生成配音……）都能一键撤回，敢撤才敢试
- **空格 = 播放/暂停，← → = 前后跳 1 秒**：手不用离开键盘看片；
  正在打字时这些键归输入框，不会打架

---

## 0.1.27

### 编导台：视觉工艺一轮打磨

- **缩略图放大近半**（62→74 宽、卡带加高）：瓶身、人脸、场景在卡上
  认得出来了——看不清的画面等于不存在
- **字号层级收敛成三档**：台词 12 / 控件 11 / 元数据与提示 10，
  比最小档还小的 9px 全数出局；「用它」等核心动作加粗提亮，
  一眼读得出「可以点」
- **该有的过渡都有了**：镜头详情展开/收起 200ms 缓出（不再硬切跳变）、
  行块点亮/选中 180ms 过渡（播放跟随换行不再闪跳）、左缘播放条
  淡入淡出

---

## 0.1.26

### 编导台：预览成为主角——播放跟随行块、进度条可拖

- **播放跟到哪，行块亮到哪**：预览播放时右栏当前行的块自动点亮
  （左缘蓝条+微亮底色）并滚到眼前——看着自己的片子走，不用自己追
- **进度条可以拖了**：预览下面的进度条支持拖动定位，拖到哪从哪继续
- **非常规速度亮出来**：放慢充满产生的 0.68x 这类速度单独显示成
  高亮档，不再让四个预设灰档冒充「没变速」
- 文案继续人话化：「未分配→还没分」「相邻镜头自动让出/补上→多退少补，
  旁边的镜头自动配合」「变速后重新框选→换速度会把起点归零」

---

## 0.1.25

### 编导台：标签接入素材库词表、找镜头三维度检索

- **标签能改了，不只是删**：行块上的标签（还有找镜头面板里的约束标签）
  点开就是标签选择器——素材库的整个标签体系按组铺开、可搜索，
  「促单」想换成别的，搜到点上就行。本任务的标签组排最前
- **找镜头检索重做成三个维度**：**按台词**（这一句说什么，就找说同类话
  的分镜——打开面板默认就用它预搜）/ **按画面描述**（人来描述想要的
  画面）/ **找相似**（拿候选卡的首帧找同款画面）。三个维度随时切
- **标签与项目成为统一约束**：不再是「按标签搜」单独一个模式——
  勾选的标签会贴在**每一种**检索维度上一起生效，不勾就是不限

---

## 0.1.24

### 编导台：素材偏短自动放慢充满；参考分镜播放修复

- **放慢充满（新）**：镜头素材比这一句的配音短时，不再只留一句
  「素材可能不够长」——自动配镜会直接把镜头放慢到刚好铺满整句；
  已有的行在警告旁多了「放慢充满」，一点就好。放慢有底线（0.5x），
  再慢画面就拖沓了，实在不够会说清还差几秒，建议换素材或加镜
- **修复**：点参考分镜的播放弹窗每次都从头放（不是从这一镜开始）——
  现在等视频真正加载完再定位，循环也只在这一镜的区间里转

---

## 0.1.23

### 编导台：一键「生成草片」——片子自己诞生

- **生成草片（新）**：顶栏新增「生成草片」。点一下：没配音的句子自动
  配上音（用全片最近用过的音色，不弹 27 次选择器）、没镜头的句子自动
  从素材库找一个配上（有标签按标签找，没标签按台词找，绝不跟别的句子
  撞素材）、时长自动分好——**全部完成后草片直接播出来**，人只做否决
  和替换
- **花钱的事先说清**：动手前一句话讲明这次要做几次语音合成、给几句
  找镜头；已经全就绪时不再花钱，直接提示按播放看草片
- **从视频提取脚本后顺路到底**：提取完成会直接问「要不要生成草片」，
  答应的话连打标签一起做完，一路播到成片预览
- **进度看得见**：生成期间中央舞台显示「正在给第 x/N 句配音/找镜头」
  和正在处理的台词，不再是转圈不说话
- 个别句子配不上（比如素材库没搜到合适的）会点名说清哪几句要手动补，
  绝不静默少一段

---

## 0.1.22

### 编导台：配乐按行铺、字幕按行改

- **配乐段（行区间铺设）**：顶栏「配乐」现在是段管理——每段选起止行、
  选曲、调音量，可加多段多曲；**相邻两段同一首曲子时播放连续不重头**。
  预览与导出都按段混流；某段曲子没下载完导出会点名拦下
- **行级字幕覆盖**：每行块底多了字幕小图标——素材自带字幕位置不同时，
  单独给这一行改样式（位置/字号/颜色/遮罩），随时可恢复跟随全局；
  已自定义的行图标是蓝色
- 旧任务的整片单曲配乐自动迁移为一段全区间，设置不丢

---

## 0.1.21

### 编导台增强五连：毛玻璃字幕、参考分镜、行级参考、素材原声

- **毛玻璃字幕遮罩（新）**：字幕样式的「遮罩」现在有三选——无 /
  **毛玻璃** / 黑底条。毛玻璃是把字幕背后那块**画面**磨砂模糊，
  不是压一条黑底（导出与切片渲染全线支持）
- **参考分镜（新）**：提取脚本时顺带做视觉切分——一句台词里有几个
  镜头，块里就有几张参考卡（参考1、参考2…），各自可播放、各自
  「用它」一键作镜头
- **行级参考视频（新）**：手写的行也能挂参考——块里点「传参考」
  上传一段视频，之后与提取的参考同等对待
- **画面行素材原声（新）**：画面行不再静音——预览和导出都用所选
  素材自己的声音（变速跟着变调整速，素材没声音才垫静音）
- 修复：编导台/审核页开着时 `ishkafel open` 唤醒没反应（Agent 唤人
  会先收回列表再进目标任务）；「未进预览」的点名在行多时按原因
  分组汇总，不再糊满中栏

---

## 0.1.20

### 编导台布局重做：行带式分镜编辑板 + 参考视频列

- **右栏彻底重做**：不再是「点一行切换一次」的检查器——现在是
  **分镜编辑板**，所有行的工作块从上到下铺开、一块对应一句台词，
  从上往下扫一遍就是扫完整条片子。每块自带：台词、参考段、镜头序列、
  配音控件；镜头详情（时长/起点/速度）点卡在块内展开，再点收起
- **参考视频列（新）**：上传成片提取脚本后，每句台词在原片里的对应
  片段自动挂在块里——带「参考」角标的缩略卡，点击小窗循环播放；
  点「用它」一键把这段原片画面填成本行第一个镜头
- 点任意块 = 选中该行 + 预览跳播到这一句（就绪时）
- 布局调整：左 = 脚本写作区（可编辑），中 = 预览（窄而居中），
  右 = 分镜编辑板（主工作区）；左右台词一一对应
- 设计推演记录见 docs/2026-08-20-编导台行带式布局-设计推演.md

---

## 0.1.19

### 脚本成片全链路打通：写脚本到导出成片，一站到底

**找镜头（新）**
- 右栏「镜头」节：点「找镜头」打开检索面板——行标签自动预填成可勾选
  的筛选条（可一键 AI 自动打标），按标签搜、按画面描述搜、点候选卡上
  的「找相似」以图搜图；别的行已用的素材会标「第 N 行在用」防撞车
- 挑中的镜头按点选顺序排进行里，选中即下载固定到本地（下载状态
  在卡片上，失败可点重试）

**时长分配（新）**
- 挑完镜头自动按行时长均分；点镜头卡展开详情：时长 ±0.5s 步进
  （相邻镜头自动让出/补上，总长锁死）、起点滑杆（框选不必从头）、
  速度四档（变速改可用量、清框重选）；素材不够长会有橙色缺口警告

**整片预览（新）**
- 中栏是真播放器了：配音与镜头就绪的行自动拼成片预览（多轨同步，
  变速镜头预渲对齐切片）；没就绪的行逐条点名原因，不静默变短

**字幕与配乐（新）**
- 顶栏「字幕样式」：位置/字号三档/六色/底条开关，整片生效
- 顶栏「配乐」：搜曲试听、调音量，预览与导出都会带上

**导出成片（新）**
- 顶栏「导出成片」：每镜按框选与变速渲染、字幕按逐字时间戳跨镜
  烧制、配音与配乐混流，产出落在桌面「ishkafel-脚本成片」文件夹；
  有没就绪的行会被逐行点名拦下，绝不悄悄少一段
- 提取脚本完成后可一键给全部台词批量打标（先确认再花钱，逐句进度）

---

## 0.1.18

### 编导台 M2：配音链路点亮

- **行级配音**：右栏配音节可选音色（豆包 2.0 音色库，按场景分组可搜）、
  调语速（0.75x / 1x / 1.25x / 1.5x），点「生成配音」出声——没选过音色
  时会先弹选择器，选完直接生成，不用点两遍
- **显式生成，改字不花钱**：改台词/换音色/调语速只把状态标为「已过期」
  （行首状态点变橙、试听条提示「这是旧配音」），旧配音仍可试听；
  点「重新生成」才调用合成服务
- **配音时长即行时长**：生成后按实际音频量出时长（试听条显示），
  这是该行时间轴的根；逐字时间戳随配音落盘，后续字幕直接用
- 行首状态点三色：灰 = 还没生成，绿 = 配音是最新的，橙 = 内容改过
- 配音文件按任务归档，删任务随目录清走；重新生成成功后才删旧音频，
  失败时旧配音完好

---

## 0.1.17

### 编导台成型：从视频提取脚本 + 全新界面；任务有了短编号

**编导台（脚本成片）**
- **从视频提取脚本**：上传一条参考成片，自动识别台词并按语义分行，
  改几个字就能用（顶栏按钮随时可用；覆盖已写内容前会确认）。提取的
  每一步（读音频 → 识别台词 → 语义分行）都有进度交代，失败给原因和重试
- **界面重做**：空脚本进来先回答「从哪里开始」——提取脚本或直接写，
  两条路两张卡；顶栏带任务编号、名字与「更改已自动保存」状态；左栏
  行列表精致化（选中蓝条、悬停才出拖柄和删除、行号对齐、尾部「新行」）；
  回车新行后光标自动落进去，手不用离开键盘
- 未配置 AI 服务时，提取入口禁用并说明原因，不再是点了没反应

**任务短编号（#1、#2…）**
- 每条任务现在有个念得出口的编号，显示在任务卡和编导台顶栏。跟 Agent
  沟通说「把 #12 导出」就行，不用再报冗长还可能重复的任务名
- 老任务自动按创建时间补号；搜索框输入「#12」或「12」直达
- 命令行全线支持：所有 `<任务 id>` 参数位置直接给 #12 也认，
  新增 `ishkafel tasks` 列出全部任务的编号对照

---

## 0.1.16

### 新模块「脚本成片」（编导台）第一步：写脚本

- 新建任务多了第四种来源——**脚本成片**：不从成片出发，从脚本出发，
  配音配镜长出成片（与「成片翻新」互为镜像）。本版先落地写脚本这一步，
  配音、找镜头、预览、导出在后续版本逐个点亮
- **编导台**（脚本任务的工作页）三栏：左栏写脚本——一行一句台词，
  回车新起一行，空行就是画面行（无台词、只有画面）；行可拖动排序、
  删除；画面行可手填时长（不填则跟随所选素材）
- 脚本**随写随存**（不存在「保存」这回事），关掉重开一字不丢
- 任务卡：脚本任务显示台词句数与「进入编导台」引导；没有原片的任务
  （拼片/脚本）右键菜单不再出现点了必失败的「重新分析」

---

## 0.1.15

### 切分编辑交互重做：边界不可拖，切割跟着播放头走

- **切割**：播放头停在哪一帧，就在那一帧切开——不再要求「先选中、且
  播放头必须落在选中对象范围内」（「播放头不在所选范围内」这条提示
  已退役）。选中视觉镜头时切镜头层，其余情况切台词语义单元层
- **边界不可再拖**：时间线上单元/镜头的边界拖拽已移除。边界修正只用
  属性面板的逐帧按钮——**按住可连发快走**，每走一帧预览画面跟着跳到
  那一帧
- 合并功能不变；有替换素材的段落照旧锁定
- 所有编辑被拦下时的提示都说清原因和下一步

---

## 0.1.14

### 全方位加固：一次产品级审视后的四批优化

**导出正确性（重要）**
- 修复：选 720P / 60fps / HEVC 导出时，替换镜头段落此前按固定的
  1080P/30fps/H.264 编码，与其他段落规格不一致会导致花屏、错位甚至
  导出失败。现在所有段落严格按所选规格渲染，字幕大小也随分辨率
- 修复：空白任务铺了配乐后无法导出的问题
- 替换镜头的字幕不再带标点符号（对齐原片字幕的习惯）

**导出提速**
- 段落并行渲染 + 素材下载/探测去重，多段落任务导出明显变快
- 增量重导：改一个候选再导出，没变的段落直接复用上次的渲染结果

**磁盘不再白占**
- 人声分离的背景轨（每条 30MB、从来没人用）不再落盘
- 变速切片、人声分离等目录纳入统一清理：删任务带走、
  「清理残留产物」可清、预览代理有了 10GB 配额

**操作体验**
- 挑好的素材保存失败会提示并可一键重试（此前静默丢失）
- 删除配乐段、强制接管任务前有确认框
- 导出进度在人声分离阶段会说明「可能要几分钟」，不再像卡死
- 导出默认目录不再记住临时目录

**Agent 能力（配合命令行工具使用时）**
- Agent 提交的方案现在会进入审核页；人审核剔除后，Agent 再导出会被
  点名拦下，绝不把剔掉的素材静默导出去
- 素材检索支持画面描述语义搜（--keyword）、翻页（--page）、
  标签自动收窄；ishkafel task 可查看方案现状；退出码规范化

---

## 0.1.13

### 修复：替换镜头字幕的居中与字号

- 字幕改为正确的居中排列（此前折行的第二行会靠右）
- 字号与原片字幕对齐（原来偏大）

---

## 0.1.12

### 改进：替换镜头的字幕逐字对齐语音，样式对标原片

- 字幕不再按「整句」显示：哪个字的发声时间落在这个镜头里，哪个字才出现
  ——跨镜头的长句前后半句各归各的镜头，不会把上一个镜头说完的话带过来
- 长句自动拆成短段先后出现（优先在标点处断开），不再堆成三行大块字
- 样式对标原片字幕：粗黑描边 + 实心白字

---

## 0.1.11

### 新增：镜头替换保留台词字幕

替换掉视觉镜头后，原片烧在画面里的台词字幕不再跟着消失：被替换的段落
会用分析时的逐句转写**重新渲染**同一句字幕（白字黑描边、底部居中），
和口播声音逐字同步。保留原片的镜头不动；一句话横跨两种镜头时，两边
同一时刻显示同一句，内容连续。预览里看到什么，导出来就是什么。

字幕文字用 macOS 系统渲染（不依赖 ffmpeg 装没装字幕组件——新版
Homebrew 的 ffmpeg 已经不带，这条路在任何机器上都能跑）。

---

## 0.1.10

### 修复 + 加固：素材库连接收口——坏了也只坏得明明白白

上一版修掉的「素材面板误报未找到 miaoa」暴露的是结构问题：每个功能各自去
找 miaoa、各自翻译报错，哪处漏了哪处坏。这一版把所有 miaoa 调用收进唯一
的网关（路径解析、错误分类只此一份），并加了一条架构测试守门——以后新代码
想绕过网关直接测试就红。用户侧的变化：

- 检索失败不再只有一句红字：登录失效直接给「重新登录」按钮，点开登录、
  登录完自动重发检索；其他失败给「重试」按钮
- 所有报错文案统一成一套（登录失效/无权限/断网/内容已删各有各的说法），
  原始报文只进日志，不摊给用户

---

## 0.1.9

### 改进：Agent 说明书粘过去就自动装成永久技能

以前说明书开头是让 Agent「自己把文件存到技能目录」——存不存全看它自觉，
聊久了就忘。现在开头改成一条命令：`ishkafel skill --install`（Claude Code /
Codex 自动认目录；别家工具加 `--dir <目录>` 自报位置）。落盘由软件保证，
装到哪命令会打印出来，Agent 把路径回你，你就知道装成了。装好之后这份
说明书就是它的永久记忆——换会话、聊多久都不会丢。

---

## 0.1.8

### 新增：审核模式——挑完的候选，一屏把关再导出

工作台底部多了「审核候选」：每个位置上「原片这一段 → 替换候选」对照摆开
（真实首帧、时长、镜头替换的变速倍率、★ 预览版标记），**悬停即播、点击
剔除**，一次「确认」后回到工作台接着导出。左侧位置栏显示各处的保留进度。

播放的是本地落好的素材文件，和导出用的是同一份——**看到的就是要交付的**。

Agent 也走同一个能力：跟它说「做完让我审核」，它跑 `ishkafel review` 把
这个页面弹出来；你确认之后告诉它继续，任务里的方案就是审核后的最终结果。
人在审核（或开着工作台）期间 Agent 写不进来，反过来 Agent 操作时人进门
会被提示——谁先进谁处理，可以强制接管。

任务列表右键任务卡也有「审核候选」，随时补审。

### 新增：说明书粘贴即装

说明书开头加了给 Agent 的自举指令：粘过去它的第一个动作就是把自己存成
技能文件（跨会话有效，不再「聊久了就忘」），并回报装到了哪个路径——
你一眼能看出它是真装了还是只读了。

### 修复

- 拖台词语义单元的边界时，被越过的**镜头切点跟着走**，不再被吞掉——
  那些是真实的画面切换，丢了等于让邻居的首镜头横跨好几个切换。转移过去
  的镜头继承原有标签。微调一帧不会再产生碎镜头
- 单元边界的切分锁收紧：两侧单元里任何挑过素材的镜头都拦（原来只查贴边
  那颗，深处的镜头挑过素材时拖边界会让素材静默串位）
- 「改完之后要连带处理吗」在拖拽松手后 0.8 秒就问，不用再等 3 秒

---

## 0.1.7

### 变化

**变速倍率不再设限。** 原来镜头替换的候选必须能在 0.8×~2.0× 内对齐坑位，
超出就拒绝导出。现在拆掉了这道闸：3 秒坑位塞 10 秒素材就按 3.3× 快放导出。

为什么可以拆：预览渲染的就是**真实倍率**的变速切片——你在预览里看到 2.9×
什么样，导出来就是什么样（同一段代码算的同一个倍率），而且镜头替换只变画面、
口播保持原片，声音不会变调。你看过并接受了，软件不该再替你做审美判断。
倍率仍然如实标在时间线与候选卡上。

---

## 0.1.6

### 新增：语义切分与打标可以交给 Agent 自己做

```bash
ishkafel analyze <task> --external=segment,tag
```

analyze 会在该 Agent 做的地方停下来，把**待办**交出去：要什么输入、有哪些
硬约束、做完怎么交回来，一次说清；Agent 做完用 `apply segment` / `apply tags`
回填，软件负责校验和组装。

这不是为了省 API 钱，是为了**让调用它的 Agent 用上自己的判断**——内置的那两步
只看得到文字和时间，Agent 能真的去抽帧看画面再打标签。待办里直接给了源片路径
和每个镜头的抽帧时间点（取中点，两端常踩在转场上）。

**ASR 不可外包**，写了会直接报错。它的时间戳偏 200ms 就毁掉整条链，而且不报错，
一路静默到看成片才发现。能外包的边界是「输出可不可验证」，不是「Agent 会不会」。

回填的东西一律校验：切分必须首尾相接、覆盖全部句子，且不接受调用方改台词文本；
标签必须逐字命中受控词表，不做近似匹配。错误一次全报出来，一批只错一条也整批不落。

`ishkafel todo <task>` 可以把欠着的待办再取一遍——输出丢了不用重跑分析（那会重跑 ASR）。

### 新增：命令行工具随包走，设置里点一下就能用

设置 → 运行环境 → 命令行工具 → 「安装」，弹一次系统授权框，之后在**任意终端**
敲 `ishkafel` 就能用。

以前这东西只能在开发机上自己编，换台电脑就得重来一遍。现在它**随 app 一起走，
Apple Silicon 与 Intel 通用**，使用者不需要知道自己这台是什么芯片，也不用下载
或编译任何东西。

app 换过位置之后命令会指向老路径，设置页会显示「需要重新安装」并说明原因——
不会让人对着一句 `no such file` 发呆。`/usr/local/bin` 里已经有别人的同名命令时
不覆盖，直接说明情况。

### 新增：Agent 说明书，复制全文给任何 Agent

设置 → 运行环境 → Agent 说明书 → 「复制全文」，粘给你的 Agent
（Claude Code、Codex、Cursor、Warp、各家桌面版都行），跟它说
「把这份技能装给你自己」。之后直接说「用 ishkafel 翻新这条片子」就行。
要发给同事就「存成文件」，微信发过去即可。

补的是最后一个缺口：装完 app 和命令行工具，Agent 知道**能调什么**，还不知道
**怎么用才做得出能用的片子**——挑素材要看前后、同一批素材几乎一样要先看图、
方案之间要有可见差异，这些没人告诉它。

为什么是复制全文而不是替你写进技能目录：技能目录每家 Agent 都不一样，
替两家装、别家不管，反而让人以为只支持那两家。说明书本身就是一份 Markdown，
Agent 认字就知道该把它装到自己哪儿。

说明书**编在二进制里**，永远跟这一版的命令对得上。命令行同样能取：
`ishkafel skill` 打印全文。

### 新增：空白任务、导出规格、差异化挑选（GUI 与 CLI 都有）

**不用原片，从素材拼（空白任务）**：新建任务多一条通道，手动加分子、
从标签组词表里选标签，按标签搜素材填进去，拼成新片。原流程只跳过
「有原片」那一步，配乐、矩阵导出全部照旧。CLI 对应 `blank create/add/remove/tags`。

**导出规格对齐剪映**：分辨率 480P~4K、帧率 24~60、码率 推荐/更高/更低/自定义
（推荐档按分辨率×帧率算出具体数字，1080P@30 = 12 Mbps）、编码 H.264/HEVC、
格式 mp4/mov，预计大小实时跟着变。CLI 对应 `export --resolution/--fps/--bitrate/
--codec/--format`。

**挑差异最大的 K 条**：组合太多时可以只导几条彼此差异明显的。距离算在素材
本身的相似度上（同一批拍摄的算「一样」），并尽量让每条素材都露一次面。

**同一条素材不能在一条成片里用两次**：挑的那一刻当场拦（说清已用在哪、
去哪儿解）；Agent 提交方案时同样点名拒绝。

**替换素材也做人声分离**：整体替换的段落铺配乐时用素材的纯人声，
不再和素材自带的背景音叠在一起。预览与导出同一条规则。

### 修复

- `apply` 跑完立刻还锁。原来要等心跳超时 60 秒，这期间人在 app 里打开这个
  任务只能看不能改，还不知道为什么

---

## 0.1.4

### 新增：命令行入口（给 Agent 用）

装了这一版之后多出一个 `ishkafel` 命令，能让 Agent（Claude Code / Codex /
任何能跑 bash 的）独立跑完整条流程：

```bash
ishkafel tag-groups                     # 当前企业有哪些标签组
ishkafel import <视频> --tag-groups 396,365
ishkafel analyze <task>                 # 抽音频 → ASR → 切分 → 打标
ishkafel task <task>                    # 任务全貌
ishkafel candidates <task> --unit 1 --shot 5
ishkafel apply plans <task> --file plans.json
ishkafel export <task> --out ~/Desktop
ishkafel open <task>                    # 把 app 弹出来转人工审核
```

构建：`./scripts/build_cli.sh`，产物在 `build/ishkafel`。
给 Agent 的操作手册在 `docs/AGENT_SKILL.md`——**没有它，CLI 只是一堆能调用
的动作，产不出能用的片子**。

几个刻意的取舍：

- **不做笛卡尔积**。Agent 提交的是完整方案列表，每条都是整体设计过的。
  笛卡尔积隐含「任意搭配都成立」，而挑素材本来就要看前后是否顺畅
- **候选带上下文**：本单元台词、相邻镜头的画面描述与它们已选的素材。只给
  「这个镜头 2.8 秒、标签是厨房清洁」，很容易挑出每一个都合规、连起来很怪的组合
- **导出前如实说代价但不设闸门**——跑不跑是调用方的判断

### 新增：任务锁

Agent 在跑而人打开 GUI，两边都写同一份任务数据会互相覆盖，而且悄无声息。
现在：谁在操作就占着锁，另一边进只读并显示是谁占着；可以强制接管；**锁在
心跳停 60 秒后自动失效**，所以持有者崩了也不会把任务永久封死。

### 修复

- `import` 没指定标签组时明确警告。标签组是打标的受控词表，缺了它后面挑
  替换素材时会没有标签可用——这条链上原本没有任何一步会报错

---

## 0.1.3

### 修复

**登录了却没有企业，界面上只字未提。** 账号登录是通的，但「租户」显示一个
「—」、项目「0 个可选」。这个状态下软件其实什么都干不了：没有企业就没有项目、
没有标签组、检索不到素材。现在：

- 登录完会**确保落到某家企业上**。只有一家的替你选掉不打扰；多家的弹出来让你
  选，且不给跳过；一家都没有会直说要找 miaoa 管理员
- 已经登录但没有企业时，设置页顶上一条橙色警告加一个「选择企业」按钮

上一版把选企业挂在了「登录成功那一刻」，于是**已经登录过的人永远碰不到**——
而那恰恰是会出问题的人所处的状态。

**切换项目只显示了一小部分。** 一个企业下有 69 个项目，界面上只列出 10 个。
两个原因叠在一起：只拉了第一页（每页 20），停用的又在本地被滤掉了。现在翻页
拉全，停用的交给服务端过滤。

**项目多了没法选。** 超过 8 个时出搜索框，可按名称或 ID 搜。本地即时过滤，
同时用 miaoa 的关键词搜索兜底。

### 变化

- 新建任务选标签组时，如果当前企业下**一个标签组都没有**，直说多半是企业选错了
  并指出去哪儿切——原来那句「可以试试标签名」在这种情况下是误导，再怎么换关键词
  都是空的
- 设置页常驻一句提醒：标签组按企业分，选不到标签组时先确认企业

### 内部

- 打包版本号每次自增，`build/dist/` 只保留最新一个包

---

## 0.1.0 及更早（未版本化的包）

> ⚠️ 这一批包**文件名相同、界面看不出区别**，是 8 月 10 日那天陆续打的。
> 版本号自增就是为了终结这件事——今天已经因此误判过一次：以为功能没打进包，
> 其实是对方装的旧包。

### 大的变化

**预览全链路走代理。** 预览用的每一段（原片、候选素材、变速切片）都转成同一个
固定规格（H.264 / 720×1280）。一举解决三件事：

- **Intel Mac 能用了**。原来所有素材要向原片规格看齐，而原片是 HEVC 10bit——
  Intel 的显卡编不了，整条链断掉
- **替换点的顿挫、加速消失了**。段与段规格一致，播放器在接缝处不必重建解码器
- **老机器预览更流畅**。720p 的解码压力比 1080×1920 的 HEVC 10bit 小一个量级

导出**不受影响**，仍然走原始素材（有测试盯死这条边界）。

**在 app 里登录 miaoa。** 设置 → miaoa 账号，手机号收验证码即可，不用再开终端
敲命令。登录状态仍由 miaoa 命令行工具保存，本应用不留任何凭据。

**缺工具就在设置里点一下装。** ffmpeg / miaoa CLI / 人声分离工具，运行环境那一页
直接给「安装」按钮，**走清华镜像**（默认源在国内基本装不上），安装过程逐行显示。

**挑过替换素材的单元/镜头，切分锁死。** 替换方案是按下标记的，切一刀、并一次
下标全变，原本挑给某个镜头的素材会跑到别的镜头上。现在这些段落不能再切分、
合并、改边界，界面上会说明是谁被锁、为什么、怎么解开。

**导出目录可选，并且记得住。** 导出确认页上能看到这个项目之前导过几次、导到哪儿，
每条都能直接打开。

### 修复

- 播到被替换的镜头时播放指针会飞到片尾
- 变速镜头播放时声音像在快进（纠偏 seek 每次都在制造下一次）
- 「正在准备 N 段变速画面」永远转下去
- 整体替换播的一直是没规格化的原始下载
